# Muliya Kaizen Project Status

## Overall Progress
Overall Completion: ~72%

The documentation set (product, design, engineering) is comprehensive and largely internally consistent (a few contradictions noted below). The codebase in both `kaizen-web` and `kaizen-api` is a **scaffold/foundation checkpoint**: tooling, auth wiring, middleware, and folder structure exist, but almost no business logic, no database schema, and no feature UI has been built yet. Documentation is far ahead of implementation.

## Completed
- **Documentation**: Product overview, roles/permissions, product requirements (with gaps — see Documentation Audit), UI spec, design system, engineering architecture, DB schema spec, API spec, folder structure spec, Cursor coding rules. ~13,000 lines total.
- **kaizen-web tooling**: Next.js 16 + React 19 + TypeScript project bootstrapped; Tailwind 4 + shadcn/ui pattern; ESLint/Prettier scripts; `components.json` configured.
- **kaizen-web auth shell**: Clerk sign-in/sign-up pages wired to real `<SignIn>`/`<SignUp>` components; `middleware.ts` protects all routes except public ones via `clerkMiddleware`/`auth.protect()`.
- **kaizen-web app shell**: Root layout, error/loading/not-found pages, dashboard layout composing `AppSidebar` + `AppHeader` + `MobileNav`, theme provider (next-themes) + TanStack Query provider + Clerk provider composed in `AppProviders`.
- **kaizen-web base UI kit**: 4 shadcn primitives (`button`, `card`, `input`, `label`), feedback components (`empty-state`, `error-state`, `loading-skeleton`, `fade-in`, toast re-export).
- **kaizen-web infra utilities (built, mostly unused so far)**: `lib/api-client.ts` (generic fetch wrapper + `ApiError`), `lib/permissions.ts` (role hierarchy checks), `lib/query-client.ts`, `constants/navigation.ts`, `constants/routes.ts`, `types/enums.ts` (12-state Kaizen status), `hooks/use-debounce.ts`, `utils/format.ts`.
- **kaizen-api tooling**: Express 5 + TypeScript (ESM) + Prisma 6 + Zod 4 bootstrapped; env validation (`config/env.ts`) covers DB/Clerk/CORS/Cloudinary/rate-limit/log-level.
- **kaizen-api middleware**: Real Clerk JWT verification (`@clerk/express`), generic Zod request validator, error handler, 404 handler, in-memory rate limiter, request logger — all functional.
- **kaizen-api constants**: Full role/permission maps and 12-state Kaizen status enum defined ahead of any schema/service using them.
- **kaizen-api infra**: Prisma client singleton, event bus (pub/sub), API error/response/pagination/kaizen-number utilities — all generic and functional, awaiting real models/services to call them.
- **kaizen-api database schema** (Milestone 1, 2026-07-07): Full `prisma/schema.prisma` written per `docs/engineering/01_DATABASE_SCHEMA.md` — 14 enums, 28 models (corrected count; see Known Issues) covering organization, users/auth, kaizen core (incl. 5W1H/5Why/benefits/attachments), review & scoring, implementation, business impact, gamification, knowledge base, notifications, platform settings, and audit/timeline. Migration `001_initial_schema` applied to the real Neon database and independently verified (all 28 tables + 14 enums + 47 FKs present; Prisma Client read/write, relational includes, cascade deletes, and unique constraints all confirmed working end-to-end).
- **Authentication & user sync** (Milestone 2, 2026-07-07): Full auth system, both repos — see the Authentication module entry below for details. Frontend: theme-aware Clerk `<SignIn>`/`<SignUp>` on proper catch-all routes, custom localization copy matching AUTH-001, fixed forgot-password flow, `useCurrentUser()` hook. Backend: Clerk webhook now verifies Svix signatures and syncs `user.created/updated/deleted` into the `users` table (with `UserGamification` row creation), a just-in-time sync fallback for local dev, real `attachUser` middleware populating `req.user` from the DB, and a real `GET/PATCH /api/v1/me`. `middleware/rbac.ts` (`requireRole`) is now actually usable for the first time since `req.user.role` is finally populated.
- **Authenticated Dashboard** (Milestone 3, 2026-07-08): `app/(dashboard)/dashboard/page.tsx` replaced the static "Setup Complete" placeholder with a real dashboard driven by `useCurrentUser()` (→ `GET /api/v1/me`). See the Dashboard module entry below for full detail.
- **Kaizen Submission Wizard** (Milestone 4, 2026-07-08): The first real business feature end-to-end. Backend: new `kaizens` module (create/get/update/delete/submit, ownership + status-gated, kaizen-number generation, submit-time validation, timeline events) plus minimal read-only `departments`/`categories` modules to unblock the wizard's dropdowns, mounted and live. `prisma/seed.ts` now actually seeds data (12 categories with icons/descriptions, one bootstrap "General" department). Frontend: 7-step wizard (`features/kaizen/*`) — Basics, Process, 5 Why, 5W1H, Benefits, Attachments (UI-only), Review & Submit — one React Hook Form + Zod instance spanning all steps, autosave (on step change + 30s interval) against real backend drafts, refresh-safe draft recovery via localStorage, and a real submit flow ending in a success screen. See the Kaizen Wizard module entry below for full detail.
- **My Ideas (Employee Portal)** (Milestone 5, 2026-07-08): Employees can now see, search, filter, sort, and inspect every Kaizen they've submitted — closing the loop the Wizard opened. Backend: `GET /api/v1/kaizens` (list, always scoped to the authenticated user, search/status/category/priority filters, 3-way sort, pagination) and `GET /api/v1/kaizens/:id/timeline`, both mounted and live. Frontend: new `/kaizen` (My Ideas list) and `/kaizen/[id]` (read-only detail) routes, a new sidebar nav item, a reusable `KaizenStatusBadge` backed by 2 new Badge variants (`warning`/`info`, wired to existing design tokens). See the Kaizen Wizard module entry below (My Ideas is tracked as part of it, not a separate module — same underlying `kaizens` domain).
- **Review Workspace** (Milestone 6, 2026-07-09): Department Managers (and, for visibility only, HR/CMD/Super Admin) can now see a role-scoped review queue, open a Kaizen's review detail page, start a review, approve/reject/request changes, and leave/resolve review comments — the workflow no longer dead-ends at `SUBMITTED`. Backend: `WorkflowService.transition()` and `AuditService.record()` (both previously 4-line empty stubs) are now real, matching the API spec's "Workflow Service (Internal)" contract for the transitions this milestone introduces (`SUBMITTED→UNDER_REVIEW`, `UNDER_REVIEW→{APPROVED,REJECTED,NEEDS_CHANGES}`) — validates the transition, updates `kaizens.status`, writes a `timeline_events` row, writes an `audit_logs` row, and emits a domain event (`kaizen.approved`/`kaizen.rejected`/`kaizen.needs_changes`, currently no-ops — no subscribers exist yet). New `reviews` module (`review.service.ts`, reusing `KaizenService.getById`/`KAIZEN_LIST_SELECT`/`toListItem`/pagination helpers rather than duplicating them) backs `GET /api/v1/reviews/queue` (new route file) and 7 new routes nested under the existing `kaizens.routes.ts`: `POST /:id/review/{start,approve,reject,needs-changes}`, `GET/POST /:id/comments`, `PATCH /:id/comments/:commentId/resolve`. Frontend: new `/review` (queue — search/status/department/category/priority filters, sort, pagination, reusing `MyIdeasPagination`/`MyIdeasSkeleton` verbatim) and `/review/[id]` (detail) routes; `KaizenDetailView` (Milestone 5) gained two small optional render-prop slots (`actionSlot`, `extraContent`) so the review detail page composes the existing detail view instead of duplicating its 5W1H/5Why/Benefits/Timeline sections; a role-aware "Review" sidebar/mobile-nav item (only shown to Department Manager and above). See the Review Workspace module entry below for full detail, including what was deliberately deferred (evaluation precondition, priority changes, threaded comment replies).
- **Employee comment visibility fix** (post-Milestone 6, 2026-07-09): Employees couldn't read reviewer comment bodies on their own Kaizens — the backend already returned them correctly (scoped to the submitter via `assertCanView`), but nothing on `/kaizen/[id]` ever mounted `ReviewCommentsPanel`. Fixed by giving `ReviewCommentsPanel` a `readOnly` prop (hides the add-comment form and resolve button, defaults to `false` so the reviewer page is unaffected) and a new small `MyIdeasDetailView` client wrapper composing `KaizenDetailView`'s existing `extraContent` slot with it. No backend change.
- **Interactive Scoring Engine** (Milestone 7, 2026-07-09): Department Managers can now evaluate a Kaizen against the 5 fixed SCORE-001 parameters (Problem Identification, Creative Thinking, Implementation, Usefulness, Maintenance/Sustainability — each 0–10, auto-summed into a `totalScore`/50 and `overallRating`/10), save a draft, and submit a final, immutable evaluation — and Approve/Reject now correctly enforce the API spec's long-deferred precondition ("Evaluation submitted with recommendation: APPROVE/REJECT"), closing the gap Milestone 6 explicitly left open. Backend: new `scoring` module (`scoring.service.ts`) backing `GET /api/v1/scoring/parameters`, `GET/PUT /api/v1/kaizens/:id/evaluation`, `POST /api/v1/kaizens/:id/evaluation/submit`, `GET /api/v1/kaizens/:id/score` — all reusing `kaizenService.getById` for view-permission checks and the same "Department Manager, same department" authorization pattern `ReviewService` already established; `prisma/seed.ts` now seeds the 5 scoring parameters; `ReviewService.approve`/`reject` (Milestone 6) gained a new `assertEvaluationRecommends` check — the one deliberate modification to already-completed-milestone code, justified because enforcing this precondition is SCORE-001's entire reason for existing. Frontend: new `EvaluationPanel` (5 parameter cards, live total/rating calculation, a dependency-free 0–10 rating control — see Architecture Decisions — recommendation/confidence/remarks, Save Draft/Submit) composed into the existing `/review/[id]` page via `KaizenDetailView`'s `actionSlot`; `ReviewActionPanel`'s Approve/Reject buttons are now disabled with an inline hint until a matching evaluation is submitted; a new read-only `ScoreSummarySection` surfaces the finalized score on the employee's own `/kaizen/[id]` page once evaluated (`GET .../score`'s "Required (scoped)" auth already includes the submitter). See the Scoring module entry below for full detail.
- **Implementation & Business Impact** (Milestone 8, 2026-07-09): The Kaizen lifecycle no longer dead-ends at `APPROVED` — a Department Manager can assign an implementation owner (from any department), that owner (or the manager) tracks progress and marks it complete, a manager/HR/CMD/Super Admin verifies it, and business impact gets recorded, closing the loop through `BUSINESS_IMPACT_RECORDED`. Backend: new `implementations` module (`GET /api/v1/implementations` list — Dept Manager/HR/CMD/Super Admin/read-only Employee, per the API spec's broader RBAC for this one endpoint — plus 6 new `/api/v1/kaizens/:id/implementation*` routes) and new `business-impact` module (`GET/POST /api/v1/kaizens/:id/business-impact`); `WorkflowService`'s transition table (Milestone 6/7) extended with `APPROVED→IMPLEMENTATION_IN_PROGRESS→IMPLEMENTATION_COMPLETED→BUSINESS_IMPACT_RECORDED`, exactly as anticipated in this file's own Technical Debt notes; a small additive `GET /api/v1/departments/:id/users` endpoint powers the "assign owner" picker (no company-wide user directory exists — same "Admin Panel doesn't exist yet" bootstrap reasoning as Milestone 4's single department). Two deliberate, minimal modifications to already-completed-milestone code, both purely additive: `KaizenService.assertCanView` (Milestone 4/5) gained an `assignedOwnerId` branch, since an implementation owner can be assigned from a different department than the Kaizen itself and would otherwise be unable to even load the Kaizen; `ReviewActionPanel` (Milestone 6) gained a "View Implementation" link for post-approval statuses. Frontend: new `/implementation` (queue) and `/implementation/[id]` (detail) routes; a new `ImplementationAssignPanel` composed into the *Review* Workspace's `/review/[id]` (via `actionSlot`, alongside the Milestone 6/7 panels) since a manager who just approved a Kaizen is already on that screen; `ImplementationProgressPanel` and `BusinessImpactPanel` are reused verbatim (not duplicated into a separate "read-only" variant) on both `/implementation/[id]` (full controls) and the employee's own `/kaizen/[id]` (same components, self-gated to read-only for a plain submitter). See the new Implementation & Business Impact module entry below for full detail.
- **Notifications & Gamification** (Milestone 9, 2026-07-09): The Kaizen lifecycle now runs all the way through `REWARD_ISSUED`, and every "notify X"/"+N points" side effect deferred since Milestone 4 is real. Backend: new `notifications` module (list/unread-count/mark-read/mark-all-read + an internal `create()` used only by event handlers), new `gamification` module (points ledger writes, achievement unlock checks against 10 seeded MVP criteria, time-windowed leaderboard snapshots for all 4 periods × 2 scopes), and new `rewards` module (`POST /kaizens/:id/rewards/issue` — Super-Admin manual retry — plus an internal `autoIssue` triggered automatically by business-impact recording). `WorkflowService`'s transition table gained `BUSINESS_IMPACT_RECORDED→REWARD_ISSUED`; `src/events/handlers/index.ts` (a 2-line no-op comment since Milestone 6) now has 7 real subscribers wired to the domain events that were already firing into the void; `kaizen.service.ts`'s `submit()` gained one additive `eventBus.emit("kaizen.submitted", ...)` line, completing a side effect Milestone 4 explicitly deferred. `prisma/seed.ts` now seeds the 10 MVP achievements and 8 platform settings. Frontend: a real notification bell in the header (links to a new `/notifications` page — no dropdown/popover primitive exists in this design system, so this avoids a new Radix dependency), a new `/leaderboard` page (period/scope toggles + an achievements grid), a `Trophy`-icon nav item visible to every role, and `RecentActivityCard` on the Dashboard upgraded from a static placeholder to real notification data. See the Notifications and Gamification module entries below for full detail, including a real O(N)-writes-per-event bug found and fixed during verification, and a documented MVP limitation around rank-based achievements in sparsely-populated departments.
- **Fixed post-Milestone-9 (2026-07-10)**: `kaizen.submitted`'s "notify department manager" side effect never actually fired in real usage — it resolved the manager via `Department.managerId`, a schema field no code path in the app ever wrote. Fixed to resolve the manager the same way `review.service.ts` already does (a `User` with `role: DEPARTMENT_MANAGER` and matching `departmentId`). See `src/events/handlers/index.ts`.
- **Administration Portal — Phase 2, Module 1** (2026-07-10): The first Phase 2 ("Enterprise Edition") module. User Management (create via a real Clerk account + invite, list/search/filter, edit role/department/active status, soft-delete with a self-delete guard), Role Management (folded into User Management — the API spec models role as a field on `PATCH /users/:id`, not a separate entity), Department Management (create/edit/soft-delete, assign a manager), and Category Management (create/edit, no delete — matches the documented API surface) are all real and Super-Admin-gated end-to-end. Platform Settings (`GET/PATCH /admin/settings`) makes the 8 `points.*`/`upload.*`/`pagination.*` values seeded in Milestone 9 editable for the first time. Branch Management was explicitly scoped out this round — no schema, API spec, or product-doc definition exists for it anywhere in this codebase; see Known Issues. See the new Administration Portal module entry below for full detail, including two real bugs found and fixed during live-DB verification.
- **Milestone 11 Chunk 1: Executive Analytics Dashboard** (2026-07-10): First chunk of "Enterprise Analytics & Knowledge Platform." New `src/modules/analytics/` backs all 6 documented `GET /api/v1/analytics/*` endpoints with real Prisma aggregations — status counts, approval/rejection rate, average review/implementation duration (derived from `timeline_events`), average score/business impact, department-vs-department point ranking, and monthly trend charts. Recharts installed; the previously-stubbed `components/charts/{line,bar,pie}-chart-card.tsx` are now real, plus a new `area-chart-card.tsx`. New `/dashboard/analytics` page (Department Manager/HR/CMD/Super Admin) and a "My Analytics" section on the existing company Dashboard (every role). "Estimated Savings" is honestly reported as a Kaizen count, not a fabricated sum, since the underlying data is free text, not a structured number. See the Analytics module entry.
- **Milestone 11 Chunk 2: Enterprise Search, Filtering & Saved Views** (2026-07-11): Global fuzzy search (Fuse.js, run server-side over RBAC-scoped candidates), a Ctrl+K command palette (native `<dialog>`, no new UI dependency), recent searches (localStorage, per-user), search-result highlighting, advanced filters (date range, score range, recommendation, employee, assigned-to) added to the Review Queue/Implementation Queue/My Ideas/Admin Users list pages with URL persistence, and Saved Views (save/rename/delete/default/Super-Admin-only share). One schema addition: a new `SavedView` table (see Database Changes below). See the new Search & Saved Views module entry.
- **Milestone 11 Chunk 3A: Enterprise Reporting Engine** (2026-07-11): 10 real report types (Executive Summary, Monthly, Department, Employee/Kaizen/Review Performance, Implementation, Business Impact, Reward, Leaderboard) generated on demand from a Report Builder reusing Chunk 2's exact filter vocabulary, rendered as a Preview (KPIs, charts, detail table, rule-based recommendations, period-over-period comparison), plus an append-only Report History and "Generate Report" deep-links from the Analytics Dashboard. Preview only — no export/download/scheduling (that's Chunk 3B). Two schema additions: `ReportType` enum + `ReportGeneration` table, and `REPORTS` added to the existing `SavedViewEntityType` enum. See the new Reports module entry.
- **Milestone 11 Chunk 3B: Enterprise Export Engine & Report Delivery** (2026-07-13): Real PDF (PDFKit, hand-rolled vector bar/line/pie charts since PDFKit has no chart primitives), Excel (ExcelJS, 3 sheets, streamed, auto-sized, frozen headers), and CSV (PapaParse, full unbounded dataset, UTF-8 BOM) exports of any of Chunk 3A's 10 report types, generated asynchronously (PENDING→PROCESSING→COMPLETED/FAILED, polled by the frontend) and downloadable from a new Download Center (`/reports/history`); Scheduled Reports (Daily/Weekly/Monthly/Quarterly/Yearly, `/reports/schedules`, executed by a new background job, notifying recipients via the existing in-app notification system once ready); Report Templates (`/reports/templates`, with favorite/pin/duplicate/recently-opened); audit logging on every report action; and export-result caching (an identical request within 10 minutes is reused, not regenerated). Three schema additions: `ReportExport`, `ReportSchedule`, `ReportTemplate` models plus their enums, and `REPORT_READY` added to `NotificationType`. See the updated Reports module entry.

## In Progress
- Nothing currently in progress — Milestone 11 Chunk 3B (Enterprise Export Engine & Report Delivery) is code-complete and verified against the live database, awaiting manual/user review before Chunk 4 starts. Remaining Milestone 11 chunks (Knowledge Base & Announcements; Activity Feed/Audit Log Viewer/Dashboard redesign; Archive/Performance/Responsive polish) and the other 5 Phase 2 modules (File Upload System, Email Notifications, User Profile & Preferences, UI Polish) haven't been started.

## Not Started
- Resuming an old DRAFT from the My Ideas list (list/detail are read-only browsing; editing a draft found via the list, rather than the single in-progress wizard session tracked in localStorage, isn't wired up) — see Kaizen Wizard module notes
- Duplicate-draft action (`POST /kaizens/:id/duplicate`) — still not implemented
- Real file uploads for Kaizen attachments (Step 6 is UI-only; no Cloudinary SDK, no `/uploads/sign`, no `POST /kaizens/:id/attachments` wiring) — the My Ideas detail page's Attachments section correctly shows "No attachments" for every Kaizen as a result
- Full Workflow Engine state machine covering all 12 `KaizenStatus` values — `WorkflowService.transition()` now covers `SUBMITTED→UNDER_REVIEW→{APPROVED,REJECTED,NEEDS_CHANGES}` (Milestone 6), `APPROVED→IMPLEMENTATION_IN_PROGRESS→IMPLEMENTATION_COMPLETED→BUSINESS_IMPACT_RECORDED` (Milestone 8), and `BUSINESS_IMPACT_RECORDED→REWARD_ISSUED` (Milestone 9); DRAFT/NEEDS_CHANGES→SUBMITTED still lives separately in `KaizenService.submit` (not migrated — see Technical Debt), and the final lifecycle tail (`REWARD_ISSUED→ARCHIVED→PUBLISHED_TO_KNOWLEDGE_BASE`) still isn't wired up — Knowledge Base territory, a later roadmap item
- Milestone 11 "Enterprise Analytics & Knowledge Platform" remaining parts: Knowledge Base, Announcements, Activity Feed, Audit Log Viewer, further Dashboard redesign, Archive, Performance (virtualization/caching), Responsive polish — Parts 1-7/9 (Analytics/Search/Filtering/Saved Views/Report Generation/Export/Scheduling) all done, see the Analytics/Search & Saved Views/Reports module entries. Email delivery for scheduled reports specifically is not built — recipients are notified in-app only (see the Reports module's Chunk 3B notes and Known Issues), since no email infrastructure exists anywhere in this codebase yet (Phase 2's separate "Email Notifications" module, also not started).
- Branch Management, scoring-parameter admin editing, announcements management — all explicitly out of scope for Administration Portal Module 1 (see its own module entry); User/Role/Department/Category Management and Platform Settings are done
- File Upload System (Cloudinary SDK to be installed when Milestone 11's Reporting/Knowledge Base chunks need it; config helper exists, no upload endpoint yet)
- Knowledge Base
- Automated tests (zero test files, no test framework installed in either repo)
- Monthly Challenges, XP/Level system, Streak System — explicitly excluded from MVP gamification scope per `01_DATABASE_SCHEMA.md`'s own note ("Basic gamification only"), even though `02_PRODUCT_REQUIREMENTS.md`'s GAMIFY-001 section describes them
- 5 of 17 backend route files registered in the barrel file are still unmounted stubs — `meRouter`, `departmentsRouter`, `categoriesRouter`, `kaizensRouter`, `reviewsRouter`, `scoringRouter`, `implementationsRouter`, `notificationsRouter`, `usersRouter`, `gamificationRouter`, and (new, Administration Portal) `adminRouter` are wired into `src/routes/index.ts` with real logic (`kaizensRouter` now also has the `rewards/issue` route added in Milestone 9, on top of the 8 implementation/business-impact routes from Milestone 8, 4 evaluation/score routes from Milestone 7, 7 review-action/comment routes from Milestone 6, and Milestone 5's `GET /` list and `GET /:id/timeline`; `usersRouter` now also has the full Admin "Users" CRUD on top of Milestone 9's 3 gamification-adjacent sub-routes; `departmentsRouter`/`categoriesRouter` gained Admin write routes on top of their existing reads; `gamificationRouter` is mounted at the v1 root, not `/gamification`, to match the API spec's unprefixed `/leaderboard`/`/achievements` paths; `adminRouter` only carries `GET/PATCH /admin/settings` — scoring-parameters/audit-logs/announcements, documented under the same API-spec "Admin" section, are not part of this module's requested scope); `business-impact, dashboard, analytics, knowledge-base, uploads, announcements` remain untouched 4-line stubs — `business-impact.routes.ts` specifically stays an unmounted stub by design: every Business Impact endpoint nests under `/kaizens/:id/business-impact`, which lives in `kaizens.routes.ts` instead (the Clerk webhook itself is real too, but it's mounted separately in `app.ts`, not part of this barrel)
- 8 of 12 frontend `features/*` domains are still empty `.gitkeep` scaffolding — `auth`, `kaizen`, `review`, `scoring`, `implementation`, and (new, Milestone 9) `notifications`, `gamification` have real content now (`review`'s, `scoring`'s, and `implementation`'s `constants`/`schemas`/`utils` subfolders are still empty — nothing needed them, since filters/status labels/validation were reused directly from `features/kaizen` or live entirely on the backend; Business Impact has no separate feature folder — it lives inside `features/implementation/`, matching the backend's own module-adjacency choice)

## Current Sprint
**Milestone 9 (Notifications & Gamification) is code-complete** as of 2026-07-09 — see the new Notifications and Gamification module entries for full detail. Remaining loose ends from earlier milestones: migration `002_add_search_vector` still hasn't been written; role assignment still depends on manually editing Clerk Dashboard metadata (no Admin UI yet). None of these block starting the next milestone.

## Remaining Milestones
*Note: milestone numbering below was set in Milestone 1 and originally sequenced "Kaizen Wizard" as #3 and had no separate Dashboard entry; item 4 originally bundled "Wizard + My Ideas retrieval" as one milestone. Actual delivery order was Dashboard (Milestone 3), Kaizen Wizard (Milestone 4), then My Ideas (Milestone 5) — the latter is item 4's second half, delivered as its own milestone rather than bundled, not a jump to item 5 (Review & Scoring) below. Each is called out explicitly rather than silently renumbered, so this list stays an accurate record of what was asked for, not just what was originally planned.*

1. **Database & Core Domain** — ~~Write the full Prisma schema~~ (done, 2026-07-07) — ~~run initial migration~~ (done, 2026-07-07, verified against the live Neon DB) — ~~seed categories + bootstrap department~~ (done, 2026-07-08); remaining: migration 002 (CHECK constraints + full-text search trigger/index), seed scoring-parameters/achievements/platform-settings once their milestones start.
2. **User Sync & RBAC** — ~~Implement Clerk webhook (signature verification + `user.created/updated/deleted` → `users` table)~~, ~~wire `AuthService`/`requireRole` end-to-end~~, ~~real `GET/PATCH /api/v1/me`~~, ~~theme-aware custom sign-in/sign-up UI~~, ~~Logout Flow with confirmation~~ (all done, 2026-07-07).
3. **Authenticated Dashboard** — ~~Replace the placeholder `/dashboard` with a real profile/stats/quick-actions view fed by `useCurrentUser()`~~ (done, 2026-07-08). Remaining: role-specific dashboard variants (Manager/HR/CMD/Admin) — explicitly deferred, single generic dashboard is correct for now.
4. **Kaizen Submission Wizard & My Ideas** — ~~Backend `kaizens` module (create/get/update/delete/submit)~~, ~~frontend 7-step wizard with autosave and draft recovery~~ (done, 2026-07-08) — ~~`GET /kaizens` list + `GET /kaizens/:id/timeline`~~, ~~My Ideas list page + read-only detail page~~ (done, 2026-07-08, Milestone 5). Remaining: resuming a draft found via the My Ideas list (today only the single most-recent wizard-session draft is resumable, via localStorage), real file uploads (Cloudinary), duplicate-draft action — all deliberately deferred, see Kaizen Wizard module notes.
5. **Review & Scoring** — ~~Review queue, comments, workflow transitions through `WorkflowService`~~ (done, 2026-07-09, Milestone 6) — ~~evaluation engine (5-parameter scoring, `evaluations`/`evaluation_scores`), Approve/Reject evaluation precondition~~ (done, 2026-07-09, Milestone 7 — see the Scoring module entry). This roadmap item is now fully complete.
6. **Implementation & Business Impact** — ~~Assignment, progress tracking, verification, business-impact recording~~ (done, 2026-07-09, Milestone 8 — see the new module entry). Reward issuance itself remains explicitly out of scope (Notifications & Gamification, item 7).
7. **Notifications & Gamification** — ~~Notification delivery, points ledger, achievements, leaderboard snapshots + refresh job~~ (done, 2026-07-09, Milestone 9 — see the Notifications/Gamification module entries). ~~Award the documented "+10 on submit" points, send the documented "notify dept manager on submit" notification~~ (done — both were intentionally deferred in Milestone 4). ~~Upgrade `RecentActivityCard` on the dashboard from a placeholder to real data~~ (done). `AnnouncementsCard` remains a placeholder — Announcements is Admin Panel scope (item 8), not Gamification.
8. **Analytics & Admin Panel** — Per-role dashboard variants belong here, reports/export, admin settings/users/departments/categories/announcements/audit logs. Real department/category management supersedes Milestone 4's bootstrap seed data.
9. **Knowledge Base & Polish** — KB publishing flow, search, responsive/dark-mode pass, accessibility pass, test coverage.

## Current Architecture
- **Frontend**: Next.js 16 (App Router), React 19, TypeScript, Tailwind CSS 4, shadcn/ui + Radix, TanStack Query, React Hook Form + Zod, Framer Motion, Sonner (toasts). Charting library specified (Recharts) in docs but **not installed**.
- **Backend**: Node.js + Express 5, TypeScript (ESM), Zod validation. No queue/Redis, no logger library, no Cloudinary SDK installed yet despite config helper. Background jobs run via plain `setInterval` (`leaderboard-refresh.job.ts`, Milestone 9) — no cron/scheduler library installed.
- **Database**: PostgreSQL (Neon), Prisma ORM — schema written and migrated (`001_initial_schema`, verified against the live DB); seeded with 12 categories + 1 bootstrap department; CHECK constraints and full-text search still pending a follow-up migration.
- **Authentication**: Clerk (frontend `@clerk/nextjs`, backend `@clerk/express`) — JWT verification real on both sides; Clerk webhook verifies signatures and syncs `users`/`user_gamification`; `req.user` (with real DB role) populated on every authenticated backend request; frontend has a theme-aware custom-branded sign-in/sign-up and a working `useCurrentUser()` hook.
- **Storage**: Cloudinary (planned) — no SDK installed, no upload endpoint.
- **Deployment (per docs, not yet configured in repo)**: Frontend → Vercel, Backend → Railway, DB → Neon PostgreSQL.
- **Repo topology**: Two independent repositories (`kaizen-web`, `kaizen-api`), not a monorepo — this matches `02_FOLDER_STRUCTURE.md` but conflicts with the single-tree diagram in `00_ARCHITECTURE.md` (see Known Issues).

## Module Status

### Authentication
Status: Complete for MVP scope (Milestone 2)
Progress: ~90%
Notes: Frontend — `/sign-in` and `/sign-up` are now proper Next.js catch-all routes (`[[...sign-in]]`/`[[...sign-up]]`), fixing a real bug where Clerk's internal multi-step navigation (password reset, verification codes) would 404 under the old flat routes. Sign-in/up widgets are theme-aware (light/dark palette recomputed from `next-themes`, since Clerk `appearance` is evaluated at render time, not CSS custom properties) and use custom `localization` copy matching AUTH-001. `/forgot-password` now redirects into Clerk's own built-in "Forgot password?" flow inside `/sign-in` (the old version mounted a second, separate `<SignIn>` instance that dead-ended — Clerk's reset flow is internal widget state, not something a second component instance can drive). `useCurrentUser()` hook + `authService.getMe/updateMe` call the real backend. A real **Logout Flow** now exists per AUTH-001: `features/auth/components/logout-button.tsx` opens a confirmation `AlertDialog` (new shadcn primitive at `components/ui/alert-dialog.tsx`, backed by newly-added dependency `@radix-ui/react-alert-dialog`) before clearing the TanStack Query cache and calling Clerk's `signOut()`; wired into the header next to `UserButton`. "Redirect Login" after logout is handled by Clerk itself via the existing `NEXT_PUBLIC_CLERK_SIGN_OUT_FALLBACK_REDIRECT_URL` env var. Backend — Clerk webhook (`/webhooks/clerk`) verifies Svix signatures via `verifyWebhook()` and syncs `user.created/updated/deleted` into `users` (+ creates the paired `user_gamification` row); `attachUser` middleware resolves `req.user` from the DB on every authenticated request, with a just-in-time Clerk API fallback sync for when the webhook hasn't fired yet (local dev without a public webhook URL, or a race right after signup); `requireRole`/RBAC is now actually exercisable since `req.user.role` is real. `GET/PATCH /api/v1/me` return/update the real profile (matching the documented response shape exactly, including the gamification summary). Known, deliberate gaps: (1) "Remember Me" checkbox from AUTH-001 has no equivalent in Clerk's prebuilt `<SignIn>` — Clerk manages session persistence via Dashboard settings, not a per-login toggle, so this requirement can't be met without abandoning the prebuilt widget; (2) Clerk's own `<UserButton>` menu still has its own unconfirmed, built-in "Sign out" item — Clerk exposes no supported API to remove or gate it, so the confirmed logout flow lives in the separate button we added, not inside UserButton itself; (3) role assignment is Clerk-Dashboard-metadata-only until the Admin Panel exists; (4) per-role dashboard redirect targets (`/manager/dashboard` etc.) are intentionally not wired since those routes don't exist yet — building them is Dashboard-milestone scope, not Auth scope.

### Database
Status: Migrated, verified, and seeded (partially)
Progress: ~60%
Notes: `prisma/schema.prisma` defines all 14 enums and 28 models from `docs/engineering/01_DATABASE_SCHEMA.md` (corrected count — see Known Issues), with documented relationships, indexes, unique constraints, and cascade rules. Migration `001_initial_schema` is applied to the live Neon database and was independently verified: all 28 tables + 14 enums + 47 FKs + 69 indexes present; Prisma Client create/read/relational-include/cascade-delete/unique-constraint behavior all confirmed working against real data (test rows written and cleaned up). The `users`, `kaizens`, `kaizen_5w1h`, `kaizen_5why`, `kaizen_benefits`, `departments`, `categories`, `kaizen_number_sequences`, and `timeline_events` tables are now all actively used. `prisma/seed.ts` (Milestone 4) seeds the 12 real categories (with icons/descriptions) and one bootstrap "General" department — the latter is a pragmatic addition, not something the docs asked for (see Known Issues). Still open: no CHECK constraints (Prisma's schema DSL can't express them — needs a raw-SQL follow-up migration), no `search_vector` generated column/trigger/GIN index (deferred to `002_add_search_vector`), and scoring-parameters/achievements/platform-settings still aren't seeded (deferred to their own milestones).

### Backend APIs
Status: Auth + Kaizen Wizard + My Ideas + Review Workspace + Scoring + Implementation/Business Impact + Notifications/Gamification/Rewards live, everything else scaffolded
Progress: ~55%
Notes: `GET /health`, `POST /webhooks/clerk`, `GET/PATCH /api/v1/me`, `GET /api/v1/departments` (+ `GET /:id/users`), `GET /api/v1/categories`, `GET /api/v1/kaizens` (list, self-scoped, search/filter/sort/paginate), `POST/GET/PATCH/DELETE /api/v1/kaizens/:id`, `POST /api/v1/kaizens/:id/submit`, `GET /api/v1/kaizens/:id/timeline`, `GET /api/v1/reviews/queue`, 7 `/api/v1/kaizens/:id/review/*`/`comments*` routes (Milestone 6), `GET /api/v1/scoring/parameters` + 4 `/api/v1/kaizens/:id/evaluation*`/`/score` routes (Milestone 7), `GET /api/v1/implementations` + 8 `/api/v1/kaizens/:id/implementation*`/`business-impact` routes (Milestone 8), and (new, Milestone 9) `GET/PATCH/POST /api/v1/notifications*` (4 routes), `GET /api/v1/leaderboard`, `GET /api/v1/achievements`, `GET /api/v1/users/:id/{achievements,points,rewards}`, `POST /api/v1/kaizens/:id/rewards/issue` are all mounted and functional. 6 other route files exist but are still 4-line stubs never wired into `src/routes/index.ts`. `src/modules/auth/*`, `src/modules/kaizens/*`, `src/modules/reviews/*`, `src/modules/scoring/*`, `src/modules/implementations/*`, `src/modules/business-impact/*`, and (new) `src/modules/notifications/*`/`src/modules/gamification/*`/`src/modules/rewards/*` are real, working modules; `departments`/`categories` are minimal read-only aside from the one users-listing route (write access is still Admin Panel scope); `src/modules/workflow/workflow.service.ts` and `src/modules/audit/audit.service.ts` (Milestone 6) are real — `WorkflowService.transition()` (extended in Milestone 8 and again in Milestone 9 — see the Workflow Engine module entry) and `AuditService.record()`, previously 4-line empty-class stubs; `src/events/handlers/index.ts` (previously a no-op comment) now has 7 real domain-event subscribers; every other `src/modules/*` folder is still just `.gitkeep`.

### Dashboard
Status: Foundation complete (Milestone 3) — single generic dashboard, no per-role variants yet
Progress: ~35%
Notes: `app/(dashboard)/dashboard/page.tsx` now renders a real `DashboardView` (`components/dashboard/`) fed entirely by `useCurrentUser()` — no new API client, reuses the Milestone 2 hook exactly as instructed. Sections: `WelcomeHeader` (personalized greeting), `StatsCards` (Total Points / Ideas Submitted / Ideas Approved / Ideas Implemented / Current Rank, `—` for null), `ProfileSummaryCard` (avatar with initials fallback, role badge, department/job title with `—` fallback), `RecentActivityCard` (Milestone 9: now fed by real `useNotifications()` data instead of a placeholder skeleton) and `AnnouncementsCard` (still intentionally placeholder — Announcements is Admin Panel scope, item 8, not built), `QuickActionsCard` (3 disabled actions with "Coming soon" tooltips). Loading state is a full skeleton (`DashboardSkeleton`); error state reuses `ErrorState` with retry; entrance animation via the existing `FadeIn` (Framer Motion) component — no new animation dependency. Four small UI primitives were added to `components/ui/` (`badge`, `avatar`, `tooltip`, `separator`) — all hand-rolled with **zero new npm packages**, since the spec explicitly said not to install unless required and none of these four needed Radix (unlike the AlertDialog added in Milestone 2, which genuinely needed focus-trap/portal behavior). Verified by rendering all components through a real (temporary, since-deleted) Next.js route with mock data covering both a fully-populated profile and an all-nulls profile — confirmed correct `—` fallbacks, correct avatar initials ("CP", "AR"), correct role badges, and no runtime/hydration errors in the rendered output; full authenticated-session verification (real Clerk login) wasn't possible in this environment (no browser automation tool available). Still not started: per-role dashboards (Manager/HR/CMD/Admin) — deliberately out of scope per the milestone brief ("build the dashboard foundation... extended in future milestones"); Recent Activity and Announcements have no real data source yet (blocked on Workflow/Admin Panel respectively).

### Kaizen Wizard (incl. My Ideas)
Status: Complete for Milestones 4–5 scope (Submission Wizard + My Ideas employee portal)
Progress: ~85%
Notes: End-to-end submission + retrieval flow, both repos.

**Submission (Milestone 4, 2026-07-08)** — Backend (`src/modules/kaizens/`): `createDraft` (auto-fills `departmentId` from the user's profile when omitted, matching the documented `POST /kaizens` contract exactly; generates the `KZN-YYYY-NNNNN` number at creation time since the schema requires it non-null even for drafts), `update` (owner-only, DRAFT/NEEDS_CHANGES-only, transactional nested upserts for 5W1H/5Why + replace-all for benefits), `getById`/`remove` (ownership + role-scoped), and `submit` (validates category/problem-statement/current-process/proposed-solution/all-six-5W1H-fields/all-five-5Why-levels/at-least-one-benefit before transitioning DRAFT→SUBMITTED, writes a `timeline_events` row). The submit transition is a narrow, single-purpose implementation living in the Kaizen service, deliberately **not** a generic `WorkflowService` — building the real state machine covering all 12 statuses is the Workflow Engine milestone's job. Two documented submit side effects are intentionally not implemented: notifying the department manager and the "+10 points" award (Notifications/Gamification milestones). Frontend (`features/kaizen/`): one React Hook Form + Zod instance spans all 7 steps (Basics → Process → 5 Why → 5W1H → Benefits → Attachments → Review), `trigger()`-based per-step validation, a draft created after Step 1 and PATCHed on every step transition plus a 30s interval (autosave), and refresh-safe recovery via a `localStorage` snapshot. "Business Impact"/"Estimated Savings" reuse `kaizen_benefits` with fixed type codes (`BUSINESS_IMPACT`, `ESTIMATED_SAVINGS`) rather than the separate `business_impacts` table (that's populated later, post-implementation, by managers/HR). Category cards use a hand-built icon registry with a fallback for anything unmapped. Two dependency-free UI primitives: `textarea`, `select`.

**My Ideas (Milestone 5, 2026-07-08)** — Backend: `GET /kaizens` (list) is deliberately scoped to `submitterId: requester.id` **always**, regardless of role — the documented API spec's fuller role-based scoping (Dept Manager sees department, HR/CMD/Super Admin see everything) is Review Workspace territory, out of scope here ("Do NOT implement reviewer workflows"). Supports comma-separated `status`, `categoryId`, `priority`, `search` (title, `problemStatement` — the closest schema equivalent to the milestone brief's "description", plus `kaizenNumber` since the doc explicitly wants that searchable too), `sort` (`newest`/`oldest`/`updated`), and `page`/`pageSize` (`limit` accepted as an alias, matching the milestone brief's example query literally). `GET /kaizens/:id/timeline` reuses the exact same ownership check as `getById`. Frontend: new `/kaizen` (My Ideas list — search with `useDebounce`, status/category/priority filters, sort, pagination, empty states for "no Kaizens at all" vs. "no results for these filters") and `/kaizen/[id]` (read-only detail: general info, 5W1H, 5 Why, benefits, attachments placeholder, timeline) routes; new sidebar nav item (`components/layout/foundation-nav.ts`, shared between `AppSidebar`/`MobileNav` rather than duplicated — the previous "My Ideas" entry in the still-unused `constants/navigation.ts` also had its href corrected from `/ideas` to `/kaizen`). Cards, not a table, per instruction to match the existing design system — nothing else in the app uses a data table yet (`components/data-table/*` are still `return null;` stubs) and cards handle mobile responsiveness more naturally. `KaizenStatusBadge` centralizes status→color via a new `KAIZEN_STATUS_BADGE_VARIANT` map in `constants/kaizen-status.ts`, using 2 new Badge variants (`warning`, `info`) wired to design tokens that already existed in `globals.css` but weren't exposed on the Badge component yet. `EmptyState` gained an optional `icon` prop (backward compatible) rather than building a one-off empty state just for this page.

Verified: full backend flow — list correctly scoped to the requester only (a second "intruder" user's Kaizen never appears), newest-first default, oldest/updated sort, status/category filters, search across title/problemStatement/kaizenNumber, pagination (page split + `limit` alias), `getById`/timeline ownership enforcement (403 for a non-owner) — all against the live database via a temporary script, every check passed. Frontend: all list/detail/filter/pagination/empty-state/skeleton components (including all 12 status badge variants and both empty-state copies, matching the milestone brief's exact required text) rendered through a real (temporary, deleted after) Next.js request with no runtime/hydration errors. Could not verify the full interactive authenticated click-through flow (typing in the search box, clicking a filter, clicking into a real Kaizen) — no browser automation tool available in this environment.

Not built, by design (explicitly out of scope per the milestone brief): reviewer actions, admin actions, editing an approved Kaizen, delete-after-submission, notifications, leaderboard, analytics. Also not built: resuming a DRAFT found via the My Ideas list (list/detail are read-only; only the single most-recent wizard-session draft is resumable, via localStorage — see Milestone 4 notes), the `POST /kaizens/:id/duplicate` endpoint, and real file uploads (the detail page's Attachments section always shows "No attachments" since nothing is ever persisted server-side yet).

### Review Workspace
Status: Complete for Milestone 6 scope (review queue, review actions, comments)
Progress: ~70%
Notes: Backend (`src/modules/reviews/`): `ReviewService.getQueue` (role-scoped — Department Manager sees their own department, HR/CMD/Super Admin see all, Employees are rejected by `requireRole("DEPARTMENT_MANAGER")` before the service ever runs; defaults to excluding `DRAFT` Kaizens unless a status filter is explicitly given; reuses `KAIZEN_LIST_SELECT`/`toListItem`/pagination helpers from the `kaizens` module rather than duplicating them), `startReview`/`approve`/`reject`/`requestChanges` (each calls `kaizenService.getById` first — reusing its view-permission check — then a private `assertCanManage` restricting the actual action to "Department Manager, same department" exactly per the API spec, then delegates the status change itself to the new `WorkflowService.transition()`), and `listComments`/`addComment`/`resolveComment` (reviewers can always comment; the submitter only while `NEEDS_CHANGES`; resolve is Department-Manager-only). Routes: `GET /api/v1/reviews/queue` (new `reviews.routes.ts`) plus 7 routes added to the existing `kaizens.routes.ts` (`POST /:id/review/{start,approve,reject,needs-changes}`, `GET/POST /:id/comments`, `PATCH /:id/comments/:commentId/resolve`). Frontend: new `/review` (queue: search, status/department/category/priority filters — department filter only shown to HR/CMD/Super Admin, since a Dept Manager's queue is already server-scoped — sort, pagination) and `/review/[id]` (detail) routes under `features/review/`; `KaizenDetailView` (Milestone 5) gained two optional slots (`actionSlot`, `extraContent`) so the review detail page reuses the same header/5W1H/5Why/Benefits/Timeline rendering instead of duplicating it, inserting a `ReviewActionPanel` (role/status-aware Start Review / Approve / Reject / Request Changes buttons, the latter three behind an `AlertDialog` confirmation with an optional notes field) and a `ReviewCommentsPanel` (list + add + resolve). A new role-aware "Review" nav item (Department Manager and above only) was added to the shared `foundation-nav.ts`.

Verified: full workflow exercised against the live database via a temporary script (mounting the real `kaizensRouter`/`reviewsRouter` with a stubbed `req.user`) — Employee blocked from the queue and from every review action (403); a Department Manager from a *different* department blocked from acting on or seeing the Kaizen in their queue; HR blocked from acting (but can still view/list it) per the spec's stricter "Department Manager only" actor scope; the full `SUBMITTED → UNDER_REVIEW → NEEDS_CHANGES` and a separate `SUBMITTED → UNDER_REVIEW → APPROVED`/`→ REJECTED` path, each confirmed via the returned status, the `timeline_events` row, and (for approve) the `audit_logs` row and `approvedAt` timestamp; an invalid transition (re-starting an already-`UNDER_REVIEW` Kaizen, approving from `NEEDS_CHANGES`) correctly rejected with `409 INVALID_STATE_TRANSITION`; comment visibility/authorship rules (submitter blocked outside `NEEDS_CHANGES`, allowed during it; manager can always comment and resolve); a `DRAFT` Kaizen confirmed to never appear in the queue. 21/21 checks passed; test data cleaned up, script deleted. `npm run typecheck`/`lint` clean in both repos (only the same 3 pre-existing, unrelated `kaizen-api` lint errors remain, now down to 1 — see Known Issues); `npm run build` (kaizen-web) succeeds with `/review` and `/review/[id]` both registered. Could not verify the interactive click-through flow in a real browser — no browser automation tool available in this environment (same disclosed limitation as every prior milestone).

Not built, by design (explicitly deferred — see Known Limitations below): `PATCH /kaizens/:id/priority`; threaded comment replies in the UI (the backend accepts `parentId`, matching the documented request shape, but the frontend always posts top-level comments); a separate review "dashboard" with its own stats/KPIs (interpreted "review dashboard" as the queue page itself, since the milestone brief gives no separate spec for reviewer-facing stats, unlike the real company Dashboard). The Approve/Reject evaluation precondition and a real "Begin Evaluation" action — both listed as deferred here as of Milestone 6 — are now built; see the Scoring module entry below.

### Scoring
Status: Complete for Milestone 7 scope (5-parameter evaluation engine)
Progress: ~75%
Notes: Backend (`src/modules/scoring/`): `ScoringService.getParameters` (5 active parameters, seeded by `prisma/seed.ts`), `getEvaluation`/`upsertEvaluation`/`submitEvaluation` (each reuses `kaizenService.getById` for its view-permission check, then the same "Department Manager, same department" authorization `ReviewService` already established — duplicated as a one-line predicate per file, matching how `kaizen.service.ts`/`review.service.ts` already each own their small `assertCanX` checks rather than sharing one), and `getScore` (aggregated, MVP: simple average across submitted evaluations — normally exactly one, per the documented "one dept manager per department" model; matches the "Pending Decisions" entry on final-score aggregation). `upsertEvaluation` validates that the score set covers exactly the active parameter set, computes `totalScore`/`overallRating` server-side (the reviewer "never performs manual calculations," per SCORE-001), and replace-alls `EvaluationScore` rows on every save (same pattern as `KaizenBenefit` in `kaizen.service.ts`). `submitEvaluation` writes a `timeline_events` row (`EVALUATION_SUBMITTED` — an enum value that had existed unused since Milestone 1) and an `audit_logs` row, and makes the evaluation permanently read-only. Routes: `GET /api/v1/scoring/parameters` (new `scoring.routes.ts`, `requireRole("DEPARTMENT_MANAGER")`) plus 4 routes added to the existing `kaizens.routes.ts`. The one modification to already-completed Milestone 6 code: `ReviewService.approve`/`reject` now call a new private `assertEvaluationRecommends`, enforcing the API spec's "Evaluation submitted with recommendation: APPROVE/REJECT" precondition that Milestone 6 explicitly deferred pending this engine.

Frontend: `EvaluationPanel` (5 parameter cards — name, description, guidelines, and a rating control — live-updating total/overall-rating, recommendation/confidence/remarks, Save Draft/Submit) composed into `/review/[id]` via `KaizenDetailView`'s existing `actionSlot`, self-gated (Department Manager, same department, status `UNDER_REVIEW`) the same way `ReviewActionPanel`/`ReviewCommentsPanel` already self-gate; a submitted evaluation renders as a read-only summary card instead. `ReviewActionPanel`'s Approve/Reject buttons are now `disabled` (with an inline hint) until `useEvaluation` confirms a submitted evaluation with the matching recommendation exists — the frontend mirror of the new backend precondition, so a manager never hits a confusing 409 by clicking a live button. A new read-only `ScoreSummarySection` (reusing `GET .../score`) is composed into the employee's own `/kaizen/[id]` via `MyIdeasDetailView`'s `extraContent`, rendering nothing until an evaluation has actually been submitted.

**Design decision — rating control**: SCORE-001 specifies "Preferred: Slider. Alternative: 10 Clickable Dots. Not: Textbox." A slider needs a new Radix dependency (not installed, not requested — milestone rules require asking first). Built the documented alternative instead: `ScoreRatingInput`, a dependency-free row of 11 buttons (0–10, matching the doc's explicit "Range 0 ↓ 10, Step 1," one more than the "10 dots" description literally suggests — the numeric range was treated as authoritative over the approximate visual description).

Verified: 22/22 checks against the live database via a temporary script — 5 parameters seeded; Employee blocked from `/scoring/parameters` (403) and from evaluating; a different-department manager blocked (403, reused `assertCanView`); Approve/Reject correctly blocked (409 `INVALID_STATE_TRANSITION`) with no evaluation, with a mismatched-recommendation draft, and with a matching-but-unsubmitted draft; wrong score count rejected (400); upsert reuses one `Evaluation` row (no duplicates) and recomputes `totalScore`/`overallRating` correctly (39/7.8, then 45/9.0 after an update); a submitted evaluation becomes read-only (edit and re-submit both correctly rejected, 409); `EVALUATION_SUBMITTED` timeline event and `audit_logs` row both confirmed written; Approve succeeds once the precondition is met; `GET .../score` returns the correct aggregate to both the manager and the submitter; the parallel Reject path (precondition blocked, then satisfied) also confirmed. A separate 8-check regression script confirmed Milestone 6 paths untouched by this milestone still work unchanged (My Ideas list, review queue, start review, add/list/resolve comments including the employee-comment-visibility fix, Needs Changes, timeline). `npm run typecheck`/`lint`/`build` clean in both repos. Could not verify the interactive click-through flow in a real browser — no browser automation tool available in this environment (same disclosed limitation as every prior milestone), though one real authenticated `GET /kaizen/[id]` request (not from this session's own test traffic) was observed succeeding with 200 in the dev server log during this session.

Not built, by design (see Known Limitations below): `PATCH /kaizens/:id/priority` (still deferred from Milestone 6, unrelated to Scoring); multi-reviewer weighted aggregation (Median/Weighted Average — doc lists these as future options, MVP uses Average, which is what's built); reviewer checklist UI items from REVIEW-001 (Problem Understood / Solution Reviewed / etc. checkboxes — not mentioned in SCORE-001's own spec, judged to be REVIEW-001 narrative flavor text rather than a concrete requirement, consistent with treating the schema-driven API spec as canonical per Known Issues).

### Implementation & Business Impact
Status: Complete for Milestone 8 scope (assignment, progress, completion, verification, business impact)
Progress: ~70%
Notes: Backend — new `src/modules/implementations/` (`ImplementationService`: `list` role-scoped per the API spec's own broader RBAC for this one endpoint — Dept Manager/own-dept, HR/CMD/Super Admin/all, **Employee/own-Kaizens-read-only** — unlike every other list/queue endpoint so far, this one doesn't block Employees outright; `getByKaizenId`/`assign`/`updateProgress`/`complete`/`verify`/`registerAttachment`) and new `src/modules/business-impact/` (`BusinessImpactService`: `get`/`record`, create-only since `business_impacts.kaizen_id` is UNIQUE and the one path into `BUSINESS_IMPACT_RECORDED` can't be revisited). `WorkflowService`'s transition table (Milestone 6/7) extended with `APPROVED→IMPLEMENTATION_IN_PROGRESS` (`assign`, timeline `IMPLEMENTATION_ASSIGNED`), `IMPLEMENTATION_IN_PROGRESS→IMPLEMENTATION_COMPLETED` (`complete`), and `IMPLEMENTATION_COMPLETED→BUSINESS_IMPACT_RECORDED` (`record`) — exactly the extension this file's own Technical Debt section predicted back in Milestone 6. `verify` doesn't change `kaizens.status` at all (the API spec gives no `Transition:` line for it — verification is a quality sub-process, not its own lifecycle stage), so it writes its own `timeline_events` (`STATUS_CHANGED`, no dedicated enum value exists for "verified") and `audit_logs` rows directly, matching `ScoringService.submitEvaluation`'s precedent for non-`WorkflowService` writes. New minimal `GET /api/v1/departments/:id/users` (added to the existing, still read-only `departments` module) powers the "assign owner" picker — there's no company-wide user directory (Admin Panel scope, not built), so this narrowly scoped addition mirrors Milestone 4's single-department bootstrap reasoning exactly.

**Two additive fixes to already-completed-milestone code, both discovered during this milestone's own verification, not assumed upfront:**
1. `KaizenService.assertCanView` (Milestone 4/5) gained a `kaizen.assignedOwnerId === requester.id` branch. An implementation owner can be assigned from *any* department (`AssignImplementationSchema.assignedDepartmentId` is independent of the Kaizen's own department) — without this, an out-of-department owner could update their own assigned implementation via the API but couldn't even load the Kaizen detail page that hosts the controls. Purely additive; every previously-valid viewer (submitter, HR/CMD/Super Admin, same-department manager) is completely unaffected — verified with a dedicated regression check.
2. `ImplementationService`/`BusinessImpactService` do **not** reuse `KaizenService.getById` for their own internal view checks (unlike `ReviewService`/`ScoringService`, which do) — they carry a local `loadKaizen`/`assertCanView` pair instead, because the *implementation* view rule (submitter, owner, HR/CMD/SuperAdmin, dept-manager-same-dept) is broader than the *Kaizen* view rule in fix #1 was scoped to fix (only `getById`/`getTimeline` needed the owner branch for page-loading purposes) — using the narrower one internally would have re-introduced the exact bug fix #1 solves, one layer down.

Frontend — new `/implementation` (queue: verification-status + department filters — no search/sort, matching `GET /implementations`'s actual documented query params exactly) and `/implementation/[id]` (detail) routes; `ImplementationAssignPanel` (department + owner pickers, due date, description) is composed into the *Review* Workspace's `/review/[id]` via its existing `actionSlot` — self-gated to `status === "APPROVED"` — since a manager who just approved a Kaizen is already looking at that exact screen, rather than requiring them to separately discover a new page; `ReviewActionPanel` (Milestone 6) gained a "View Implementation" link once a Kaizen moves past `APPROVED`. `ImplementationProgressPanel` (progress bar, update-progress form, Mark Complete, Verify) and `BusinessImpactPanel` (record form / read-only summary) are each used in **two** places without any separate "read-only" variant: full read-write on `/implementation/[id]`, and reused verbatim on the employee's own `/kaizen/[id]` (via `MyIdeasDetailView`) — both already self-gate their write controls to owner/manager/HR-CMD-SuperAdmin and simply render the read-only parts (or nothing at all, pre-assignment) for a plain submitter.

**Design decision — rating/file-upload UI**: no attachment-upload widget was built on the frontend (same reasoning as Kaizen attachments since Milestone 4) — there's no Cloudinary SDK, no signed-upload endpoint, so a picker would have nowhere real to upload to, which the milestone's own "no placeholder code" rule rules out. `POST /kaizens/:id/implementation/attachments` (register metadata for an already-uploaded file) is fully implemented and verified on the backend; existing attachments are displayed as links wherever `ImplementationProgressPanel` renders.

Verified: 31/31 checks against the live database via a temporary script — owner picker returns department users; Employee/wrong-department-manager blocked from assigning (403); double-assign rejected (409); `Kaizen.assignedOwnerId`/`implementationDueDate` set correctly on assign; queue correctly scoped for Employee (own only)/wrong-dept-manager (excluded)/HR (all); a cross-department owner can view and update progress and register an attachment (this is what surfaced fix #1 and #2 above — first attempt failed with a 403, root-caused, fixed, re-verified passing); a non-owner/non-manager submitter blocked from updating; Mark Complete transitions correctly; wrong-department manager blocked from verifying, HR succeeds; `IMPLEMENTATION_ASSIGNED`/`IMPLEMENTATION_COMPLETED`/`STATUS_CHANGED` timeline events and both audit log actions confirmed written; Employee blocked from recording business impact (403); record transitions to `BUSINESS_IMPACT_RECORDED`; duplicate record rejected (409); submitter can view the recorded impact. A follow-up 3-check script specifically confirmed fix #1 in isolation (cross-department owner can now `GET /kaizens/:id` and `.../timeline`; an unrelated cross-department employee is still correctly blocked — no regression). A separate 11-check regression script confirmed Milestones 5–7 paths untouched by this milestone still work unchanged (My Ideas, review queue, start review, comments incl. the employee-visibility fix, scoring parameters, the Approve precondition, timeline). `npm run typecheck`/`lint`/`build` clean in both repos. Hit the same ~15s transient Neon cold-start delay noted in every prior milestone's verification session; resolved on retry, unrelated to any code. Could not verify the interactive click-through flow in a real browser — no browser automation tool available in this environment (same disclosed limitation as every prior milestone).

Not built, by design (see Known Limitations below): real file uploads for implementation evidence (registration endpoint works, no upload UI — see Design Decision above). Reward issuance (`POST /kaizens/:id/rewards/issue`, `BUSINESS_IMPACT_RECORDED→REWARD_ISSUED`) is now built — see the Gamification module entry (Milestone 9). Archive/knowledge-base publish (`REWARD_ISSUED→ARCHIVED→PUBLISHED_TO_KNOWLEDGE_BASE`) remains a later roadmap item.

### Workflow Engine
Status: Extended (Milestone 9) — review-workspace + implementation-lifecycle + reward-issuance transitions
Progress: ~45%
Notes: `workflow.service.ts` was a 4-line empty class ("will be implemented in the workflow phase") until Milestone 6, which gave it a real `transition(kaizenId, toStatus, actor, metadata)` matching the API spec's "Workflow Service (Internal)" contract (validate the transition, update `kaizens.status`, write a `timeline_events` row, write an `audit_logs` row via the also-newly-implemented `AuditService.record()`, emit a domain event). Milestone 8 extended its transition table exactly as this file's own Technical Debt note anticipated: `APPROVED→IMPLEMENTATION_IN_PROGRESS→IMPLEMENTATION_COMPLETED→BUSINESS_IMPACT_RECORDED`, on top of Milestone 6/7's `SUBMITTED→UNDER_REVIEW→{APPROVED,REJECTED,NEEDS_CHANGES}` — the type/table/map names were generalized from "review-specific" to generic (`ReviewableStatus`→`TransitionableStatus`, `REVIEW_TRANSITIONS`→`STATUS_TRANSITIONS`) as part of this, purely a rename with no behavior change to the existing entries. Milestone 9 extended it once more with `BUSINESS_IMPACT_RECORDED→REWARD_ISSUED` (`RewardService.performIssue`, timeline event `REWARD_ISSUED`, domain event `reward.issued`) — the domain-event map (`DOMAIN_EVENT`) that had been firing every transition's event into a no-op `eventBus` since Milestone 6 now has real subscribers for every entry (see `src/events/handlers/index.ts`). `KaizenService.submit`'s `DRAFT`/`NEEDS_CHANGES`→`SUBMITTED` transition (Milestone 4) remains deliberately un-migrated (see Technical Debt) — still out of scope, not because it's harder now, just because refactoring a completed milestone's working code still isn't what any milestone since has asked for; it does, however, now emit its own `kaizen.submitted` domain event directly (Milestone 9), completing the domain-event coverage without migrating the transition itself. The final lifecycle tail (`REWARD_ISSUED→ARCHIVED→PUBLISHED_TO_KNOWLEDGE_BASE`) still has no transition-table entries — Knowledge Base territory, a later roadmap item.

### Notifications
Status: Complete for Milestone 9 scope (in-app notifications)
Progress: ~70%
Notes: Backend (`src/modules/notifications/`): `NotificationService.list` (self-scoped, `isRead` filter, pagination), `getUnreadCount`, `markRead` (own-notifications-only), `markAllRead` (`{markedRead: N}`), and an internal `create()` — the only write path into the table, called exclusively from `src/events/handlers/index.ts`, never exposed over HTTP directly (matches the API spec, which documents no `POST /notifications` endpoint). Routes: `GET /api/v1/notifications`, `GET .../unread-count`, `PATCH .../:id/read`, `POST .../read-all` (new `notifications.routes.ts`, built out from its 3-line stub). Frontend: `NotificationBell` in `AppHeader` (unread-count badge, polls every 30s, links straight to `/notifications` — no dropdown/popover primitive exists in this design system, only `alert-dialog`/`label`/`slot`, so this avoids adding a new `@radix-ui` dependency for a preview panel); a new `/notifications` page (list, mark-read per item, mark-all-read); `RecentActivityCard` on the Dashboard (Milestone 3) upgraded from a static skeleton placeholder to real data via the same `useNotifications()` hook.

Every notification is created by one of 7 event handlers (see the Gamification module entry for the full trigger list) — there is no user-facing "create notification" action anywhere in this system, matching the domain-event-driven design the API spec's "Domain Events" table specifies.

### Analytics
Status: Complete for Milestone 11 Chunk 1 scope (Executive/Department/Employee analytics — Parts 1-3 of "Enterprise Analytics & Knowledge Platform")
Progress: ~50%
Notes: New `src/modules/analytics/analytics.service.ts` backs all 6 documented `GET /api/v1/analytics/*` endpoints (`overview`, `departments`, `employees`, `kaizens`, `trends`, `personal`) — every aggregation is a real Prisma query/groupBy over `Kaizen`/`Evaluation`/`Implementation`/`BusinessImpact`/`Reward`/`PointsLedger`, no mocked data. Reuses `GamificationService.getLeaderboard` for the employee leaderboard preview rather than re-deriving rankings; department-vs-department ranking, review/implementation duration, and savings-trend bucketing are genuinely new (nothing else in the codebase computed these). `components/charts/{line,bar,pie}-chart-card.tsx` (previously `return null;` stubs) and a new `area-chart-card.tsx` are now real Recharts wrappers, styled from the app's existing CSS custom-property palette so they track light/dark theme automatically. New `/dashboard/analytics` page (Department Manager/HR/CMD/Super Admin, matching Part 1's explicit visibility list — narrower than the API spec's own "HR, CMD, Super Admin" line for the company-wide overview specifically, reconciled by giving Department Manager only the "By Department" tab, scoped to their own department, never the Executive Overview tab) with a role-based tab switcher; a new "My Analytics" section added to the existing company Dashboard (`/dashboard`, visible to every role including Employees) for Part 3's "Employee Profile Dashboard," since `GET /analytics/personal` is self-scoped for any authenticated user, not gated to Manager+.

**A genuine data-model gap, not a bug**: "Estimated Savings" (Part 1's business metrics, and the product doc's "Estimated Money Saved") has no structured numeric field anywhere in the schema — it's free text typed into the Kaizen Wizard's Benefits step (`kaizen_benefits.description` for `benefitType = 'ESTIMATED_SAVINGS'`, established in Milestone 4). Parsing that free text into a number would be fabricated precision, which "no fake data" rules out — so every "Estimated Savings" metric in this chunk is a **count** of Kaizens that recorded one, not a summed amount, clearly labeled as such in the UI. "Actual Savings" (`BusinessImpact.moneySaved`, recorded after implementation) is a real structured number and is summed normally.

Verified: 20/20 checks against the live database (19 assertions passed outright; the 1 apparent failure was a miscalibrated test assertion, not an app bug — a test Kaizen's business-impact recording auto-cascaded all the way to `REWARD_ISSUED` per Milestone 9's own existing auto-reward-issuance flow, and `statusCounts` correctly reflected that real state) — RBAC across Employee/Department Manager/HR for every endpoint, department-scoped exact assertions (status counts, approval rate, average score, savings, top employees) against an isolated test department, company-wide overview's contains-at-least assertions against real coexisting data, self-scoped personal analytics, and role-scoped Kaizen analytics (Employee sees only their own, Department Manager their department). `npm run typecheck`/`lint`/`build` clean in both repos.

Not built, by design (Milestone 11's own chunk boundary, not this module's scope): Knowledge Base/Announcements (Parts 8-9), Activity Feed/Audit Log Viewer/Dashboard redesign beyond the "My Analytics" addition (Parts 10-12), Archive/Performance/Responsive polish (Parts 13-15) — all later chunks of the same milestone, staged like the Administration Portal (implement, verify, report, stop for review before the next chunk). Reporting exports (Part 7) are done — see the Reports module entry.

### Search & Saved Views
Status: Complete for Milestone 11 Chunk 2 scope (Global Search, Advanced Filtering, Saved Views, Command Palette, Recent Searches, Search Highlighting — Parts 1-7 of "Enterprise Analytics & Knowledge Platform"; Search Suggestions' "popular searches" explicitly trimmed, see Known Issues)
Progress: ~55%
Notes: New `src/modules/search/search.service.ts` (`GET /api/v1/search?q=`) runs Fuse.js (approved as a new dependency) **server-side** over RBAC-scoped candidate rows fetched from Postgres — Kaizens follow the exact same visibility rule as every other Kaizen list endpoint (Employee: own only, Department Manager: own department, HR/CMD/Super Admin: all); Users/Departments/Categories groups are Super-Admin-only, since the only pages that could ever display them (`/admin/*`) are themselves Super-Admin-gated — a search result never links somewhere the viewer can't open. New `src/modules/saved-views/` (list/create/update/delete; `isDefault` unsets any prior default for that user+entityType in a transaction; `isShared` is rejected with 403 for anyone but Super Admin) backs `GET/POST/PATCH/DELETE /api/v1/saved-views*`, storing each view's filters as opaque JSON mirroring that endpoint's own query params. `review.schema.ts`/`implementation.schema.ts`/`kaizen.schema.ts`'s list query schemas gained `dateFrom`/`dateTo` (all three) plus `scoreMin`/`scoreMax`/`recommendation`/`submitterId`/`assignedReviewerId` (Review Queue only) and `kaizenStatus` (Implementation Queue only) — each additive, so every pre-existing unfiltered call is unaffected (verified by regression).

Frontend: new `features/search/` — a Ctrl+K/Cmd+K command palette (`components/charts` precedent of "no new UI library" extended here: the native `<dialog>` element gives a real modal, focus handling, and Escape-to-close for free, avoiding a `cmdk`/Radix Dialog dependency for one component), a header search-bar trigger, recent searches (`localStorage`, keyed per Clerk user id — not a new DB table, since this is a convenience list a user can always just retype), and `<HighlightedText>` rendering the backend's Fuse.js match-index ranges as `<mark>`. New `features/saved-views/` — a `SavedViewsBar` (pill list, inline "Save current filters" form, rename via `window.prompt`, delete via `window.confirm` — a deliberately lightweight choice over a full `AlertDialog` for a low-stakes, easily-recreated personal shortcut) reused verbatim across the 4 pages that support it. New shared `hooks/use-url-filters.ts` syncs a page's filter state to the URL query string (Part 2's "URL should preserve filters") and is now used by My Ideas, Review Queue, Implementation Queue, and the Admin Users list — each gained an "Advanced Filters" section (Review Queue) or new fields (date range everywhere; Implementation Status on the Implementation Queue) wired through it.

**One schema change** (`prisma/schema.prisma`, migration `add_saved_views`): a new `SavedView` table (`id, userId, entityType, name, filters (Json), isDefault, isShared, createdAt, updatedAt`) plus a `SavedViewEntityType` enum (`KAIZEN_LIST`, `REVIEW_QUEUE`, `IMPLEMENTATION_QUEUE`, `ADMIN_USERS`). Purely additive (new table, zero existing rows affected) — Saved Views cannot exist without persisted state, so this was judged genuinely unavoidable rather than worked around.

**Known, deliberate scope trims**: the "Employee"/"Assigned To" pickers on the Review Queue's Advanced Filters only populate once a single department is known (reuses `useDepartmentUsers`, the exact same "assign owner" picker from Milestone 8 — no new "list every employee company-wide" endpoint was built for this). "Popular searches" (Part 6) has no tracking infrastructure — no additional table was added purely for a search-suggestion polish feature — so suggestions cover recent searches + live matching results only, not a popularity ranking. Search highlighting only marks matches found in the displayed title field, not in subtitle/other matched fields (Part 7's own example is title-only). Candidate fetch caps (2000 Kaizens, 1000 Users) are generous at Muliya's actual scale but not infinite — see Technical Debt.

Verified: 24/24 checks against the live database — fuzzy typo tolerance ("printr" → "Printer Issue"), highlight-match indices present, RBAC on search (cross-department Employee excluded, same-department Manager included, Users/Departments groups empty for HR but populated for Super Admin), validation (empty query rejected), every new filter param on the Review Queue and My Ideas (date range, submitter, future-date-excludes-everything), and the full Saved Views lifecycle (create/default, list, rename, share-forbidden-for-non-Super-Admin, delete-forbidden-for-non-owner, Super-Admin share visible to other users, owner delete). A separate 13-check regression script confirmed the full Kaizen lifecycle (submit→approve→implement→business-impact→reward cascade) and the untouched Notifications/Leaderboard/Analytics endpoints still work exactly as before with the new optional filter params absent. `npm run typecheck`/`lint`/`build` clean in both repos.

### Reports
Status: Complete for Milestone 11 Chunk 3A+3B scope (10 report types, Report Builder, Preview, History, Dashboard-integration deep links, PDF/Excel/CSV export, Download Center, Scheduled Reports, Report Templates)
Progress: ~65%
Notes: New `src/modules/reports/report.service.ts` composes 10 report builders entirely from already-verified building blocks rather than re-querying the same data a second way: `analyticsService.getOverview()`/`getDepartmentAnalytics()` back Executive Summary/Monthly/Department; `gamificationService.getLeaderboard()` backs Reward/Leaderboard; a handful of `AnalyticsService`'s private aggregation helpers (`getAvgReviewTimeHours`, `getAvgImplementationTimeDays`, `getAvgScore`, `getAvgBusinessImpact`, `getActualSavings`, `getKaizensWithEstimatedSavingsCount`, `getMonthlyKaizenTrend`, `getDepartmentSubmissionsChart`, `getSavingsTrendChart`, `getTopDepartments`, `scopeForRequester`) were flipped to `public` (pure visibility change, zero logic change) so `ReportService` could call them directly. Every report type populates one uniform `ReportResult` envelope (`kpis`, `charts`, `table`, `summary`, `recommendations`, `comparison`) rather than 10 bespoke shapes. A new `buildKaizenWhere()` filter builder mirrors Chunk 2's exact filter vocabulary (`dateFrom`/`dateTo`/`departmentId`/`categoryId`/`priority`/`status`/`employeeId`/`reviewerId`/`implementationOwnerId`, plus two new derived filters — `rewardStatus` from `Kaizen.status === REWARD_ISSUED`, `businessImpactStatus` from whether a `BusinessImpact` row exists) rather than sharing literal code with `review.service.ts`/`kaizen.service.ts` (which "do not refactor unrelated modules" rules out touching). Recommendations (Part 7) are pure rule-based threshold logic — approval rate < 40%, implementation backlog (pending > complete × 1.5), avg review time > 72h, comparison-period submission decline > 15% — explicitly **not** AI-generated, per instruction. Comparison (Part 8) computes `[from, to)` windows for MONTH/QUARTER/YEAR at the current vs. previous period, returning `difference`/`percentChange` per metric (null-safe against divide-by-zero). Report History (Part 10) is an append-only `ReportGeneration` row per generation (user/reportType/filters/durationMs) — never the report's own output content, since nothing is persisted or downloadable yet. Routes: `POST /api/v1/reports/generate`, `GET /api/v1/reports/history` (new `reports.routes.ts`, `requireRole("DEPARTMENT_MANAGER")` at the route level; finer per-report-type RBAC — Employee blocked entirely, Department Manager restricted to the literal `DEPARTMENT` report type only, HR/CMD/Super Admin unrestricted — lives in the service per Part 12).

Frontend: new `features/reports/` — `ReportBuilder` (all 12 Part 3 filter fields, reusing `useDepartments`/`useCategories`/`useDepartmentUsers` from Chunk 2/Milestone 8 and `SavedViewsBar` with a new `entityType="REPORTS"`; the report-type dropdown is restricted to only "Department Report" when the current user is a Department Manager, mirroring the backend precondition so the UI never offers a button that would 403), `ReportPreview` (KPI grid, `ReportComparison` current/previous/difference/%-change cards, charts via existing `PieChartCard`/`BarChartCard`/`AreaChartCard`/`LineChartCard` — "pie" and "donut" both map to the same already-donut-shaped `PieChartCard` from Chunk 1, a deliberate simplification not a missing distinction — a generic `ReportTable`, summary, recommendations), `ReportHistory` (paginated, reusing `MyIdeasPagination`). New `/reports` page (wrapped in a `ReportsGuard` mirroring `AnalyticsGuard`) and a role-gated "Reports" nav item (same `canReview(role)` visibility as Review/Implementation/Analytics). Part 11 dashboard integration: "Generate Report" buttons on the Executive Analytics Dashboard (prefills `reportType=EXECUTIVE_SUMMARY` + the current date range) and each Department Analytics card (prefills `reportType=DEPARTMENT&departmentId=...`) deep-link into `/reports`, where a one-time `initialFiltersFromUrl()` read on mount prefills the builder (not a live two-way URL sync, since a Report Builder's in-progress edits shouldn't rewrite the address bar the way Chunk 2's list-page filters do).

**A real bug found and fixed during this chunk's own live-DB verification, not assumed upfront**: the Implementation Report's "Completed" KPI (and its "Ideas Assigned"/"Success Rate" derivations and its detail table) initially reused `AnalyticsService.getStatusCounts()`'s `implementationComplete` count — which matches `Kaizen.status === "IMPLEMENTATION_COMPLETED"` **exactly**. That's correct for the Analytics Dashboard's live pipeline-funnel snapshot (an already-shipped, already-verified Chunk 1 semantic, left untouched), but wrong for a report whose entire subject is "did the implementation get completed" — a Kaizen that has since progressed to `BUSINESS_IMPACT_RECORDED`/`REWARD_ISSUED`/`ARCHIVED`/`PUBLISHED_TO_KNOWLEDGE_BASE` (the normal, common case once business impact is recorded and the reward auto-issues, per Milestone 9's existing cascade) objectively did complete its implementation but fell out of the exact-match count. Verified live: a fully-completed test Kaizen that had progressed all the way to `REWARD_ISSUED` reported 0 completed implementations. Fixed with a new `IMPLEMENTATION_COMPLETE_OR_LATER` status list local to `buildImplementationReport` (not a change to `AnalyticsService`, to avoid altering the Analytics Dashboard's own already-correct funnel-stage semantics) — re-verified with the same fixture, now correctly reporting 1 completed.

Verified: 22/22 checks against the live database via a temporary script — Employee blocked from `/reports/generate` and `/reports/history` entirely (403); Department Manager blocked from every report type except `DEPARTMENT` (403 on Executive Summary, 201 on Department, correctly scoped to their own department); HR/CMD/Super Admin succeed on every report type; all 10 report types generate with real KPIs/charts/tables/summary/recommendations against a full-lifecycle test Kaizen (submitted → evaluated → approved → implemented → business impact recorded → auto-rewarded); comparison metrics return the documented `currentValue`/`previousValue`/`difference`/`percentChange` shape; Report History records the correct user/duration and is self-scoped (Employee still blocked). A separate 11-check regression script confirmed Analytics (including the new `avgReviewTimeHours` field), Search, Saved Views (pre-existing `KAIZEN_LIST` entity type unaffected by the new `REPORTS` one), Review Queue, Implementation Queue, Admin (Users/Departments), Notifications, and Leaderboard all still respond correctly, unaffected by this chunk's changes. `npm run typecheck`/`lint`/`build` clean in both repos (the one pre-existing `kaizen-api` lint error is untouched and unrelated). Could not verify the interactive click-through flow in a real browser — no browser automation tool available in this environment (same disclosed limitation as every prior milestone).

**Milestone 11 Chunk 3B: Enterprise Export Engine & Report Delivery** (2026-07-13) — closes every gap the Chunk 3A entry above lists as deferred.

Backend: new `src/lib/cloudinary-client.ts` wraps the real `cloudinary` SDK (`uploader.upload_stream`/`destroy`, `resource_type: "raw"` since PDFs/XLSX/CSV aren't images) — but this environment's `.env` has `CLOUDINARY_CLOUD_NAME`/`API_KEY`/`API_SECRET` present as **empty strings**, so no real Cloudinary account exists to upload to (asked the user directly rather than guessing, given how central `downloadUrl` is to this entire chunk — see Known Issues). Resolved with a same-origin fallback: `getCloudinaryConfig()` returning `null` makes the export pipeline store the generated bytes directly in a new `ReportExport.fileData Bytes?` column instead of a `cloudinaryUrl`; the download endpoint transparently serves whichever is populated (`302` redirect to Cloudinary, or a direct byte stream otherwise) — real files, real Neon-backed records, fully live-verifiable today, and the Cloudinary path activates automatically the moment real credentials are added, with zero code changes.

Three new modules compose Chunk 3A's engine rather than re-deriving report data: `src/modules/report-exports/` (`pdf-builder.ts` — PDFKit, with a small hand-rolled vector chart layer in `pdf-charts.ts` since PDFKit has no bar/line/pie primitives — polygon-fan-approximated pie/donut slices, horizontal bars for long labels, polyline area/line charts; `excel-builder.ts` — ExcelJS's true streaming `WorkbookWriter`, 3 sheets (Summary/Charts Data/a detail sheet named "Business Impact"/"Rewards"/"Detailed Rows" depending on report type — see the Architecture Decisions note below); `csv-builder.ts` — PapaParse `unparse` with a UTF-8 BOM; `report-export.service.ts` — the orchestrator: RBAC via one call to `ReportService.generateReport` — now accepting an optional `detailRowLimit` param, defaulting to the Preview's 100-row cap, so the very same call that builds Preview-parity data for PDF can also ask for CSV/Excel's fuller 10,000-row cap without a second, duplicate aggregation query — SHA-256 filter-hash-keyed export caching (10-minute window), async PENDING→PROCESSING→COMPLETED/FAILED status with the frontend polling for progress), `src/modules/report-schedules/` (CRUD, `computeNextRun()` calendar-aware date math for Daily/Weekly/Monthly/Quarterly/Yearly, recipients as real Users rather than emails — see Known Issues), and `src/modules/report-templates/` (CRUD + favorite/pin/duplicate/`markUsed`-on-apply for "Recently Opened"). New background job `src/jobs/report-schedule.job.ts` (same `setInterval` pattern as `leaderboard-refresh.job.ts`, 60s tick) executes every due, enabled schedule through `ReportExportService.createScheduledExport`, then notifies each recipient via the existing Milestone 9 notification system (`NotificationType.REPORT_READY`, additive enum value) — no email was sent, since no email infrastructure exists anywhere in this codebase (Phase 2's own separate "Email Notifications" module is still not started). Routes: `POST/GET /reports/exports`, `GET /reports/exports/:id`, `GET /reports/exports/:id/download`, `DELETE /reports/exports/:id`; `POST/GET/PATCH/DELETE /reports/schedules*`; `POST/GET/PATCH/DELETE /reports/templates*` plus `/favorite`, `/pin`, `/apply`, `/duplicate` sub-routes — all sharing the same `requireRole("DEPARTMENT_MANAGER")` floor as Chunk 3A, with `ReportService.assertCanGenerateReportType` (a new public delegate to the existing private `assertCanGenerate`) reused by all three new modules for the finer "Department Manager: Department reports only" rule (Part 11) — one rule, enforced identically everywhere, no copies. Audit logging (Part 12) added at each real action: `report.generated` (one new line inside Chunk 3A's existing `generateReport`, now covering both Preview and every export), `report.downloaded`, `report.deleted`, `report.schedule.{created,edited,removed}`.

Frontend: `ReportExportButtons` (PDF/Excel/CSV, on the Report Preview, using the exact filters that produced the visible Preview — Part 1's "export exactly what the preview displays"), a new `/reports/history` Download Center (generated-by/date/type/status/size, download via an authenticated blob-fetch since downloads need a Bearer token a plain `<a href>` can't send, delete), a new `/reports/schedules` page (create/list/enable-disable/delete, a department-scoped recipient checklist reusing Milestone 8's `useDepartmentUsers` rather than a new company-wide directory — same scope trim already established for the Review Queue's filters in Chunk 2), a new `/reports/templates` page (favorite/pin/duplicate/apply, "Recently Opened" section), a `ReportsSubNav` linking all four Reports pages (no new top-level sidebar items — these are sub-features of the one existing "Reports" nav entry), and a `SaveTemplateButton` on the Builder. `NotificationItem` gained a `REPORT_READY` icon/href (routes to the Download Center rather than a direct file link, for the same Bearer-token reason).

**Two real bugs found and fixed during this chunk's own live-DB verification, not assumed upfront:**
1. `ReportExportService.createExport` originally created the `ReportExport` row and returned `201 PENDING` **before** any permission check ran — the actual RBAC (`reportService.assertCanGenerate`) only happened inside the async background `process()` step afterward, which just flipped the row to `FAILED` rather than rejecting the HTTP request. Caught live: a Department Manager's `POST /reports/exports` for `EXECUTIVE_SUMMARY` returned `201` instead of the expected `403`. Fixed by calling `reportService.assertCanGenerateReportType` synchronously at the top of `createExport`, before any row is created — matching how every other endpoint in this app rejects synchronously rather than "succeeding" and failing silently in the background.
2. Not a code bug, but a verification-script-timing false alarm worth recording: the regression script's `business_impact.recorded → reward.issued` notification check initially failed at a 1.2s wait, then again at 4s, before passing cleanly at 10s — the full event cascade (this chunk didn't touch it) was simply slower than usual under the Neon connection state at the time. Consistent with this project's several previously-documented Neon cold-start delays; not a Chunk 3B regression.

**Architecture decision — Excel's "Business Impact"/"Rewards" sheets**: Part 3's example sheet list (Summary/Charts Data/Detailed Rows/Business Impact/Rewards) reads as illustrative of what a report's own detail data might contain, not a rigid 5-sheet requirement for every report type — `BUSINESS_IMPACT`/`REWARD` reports' own detail table already *is* business-impact/reward-shaped data, so their one detail sheet is named accordingly rather than adding a 4th/5th empty placeholder sheet with no distinct underlying data for report types that don't produce it (which "no placeholder implementations" rules out).

**Architecture decision — PDF row cap**: unlike CSV/Excel (full dataset, up to a 10,000-row cap), the PDF detail table stays at the Preview's 100-row cap with a "see the Excel/CSV export for the full dataset" note once truncated — a deliberate print-formatting choice (a "professional" multi-thousand-row PDF isn't a real deliverable), not an oversight.

Verified: 35/35 checks against the live database via a temporary script — full RBAC matrix for exports/schedules/templates (Employee blocked everywhere; Department Manager restricted to `DEPARTMENT`-only for exports and schedules; HR/CMD/Super Admin unrestricted); all 3 export formats complete with real, non-trivial file sizes; a downloaded PDF's bytes independently confirmed to start with the real `%PDF-` header; cross-user download blocked (403); identical repeat export request reused via cache instead of regenerated; Download Center list/delete; full Template lifecycle (create/favorite/pin/apply-stamps-lastUsedAt/duplicate/pinned-first ordering); full Schedule lifecycle (create/invalid-recipient-rejected-400/list/forced-due-execution via one synchronous pass of the real job function/`lastRunAt`+`nextRunAt` update/real `ReportExport` row created/recipient notified/disable/delete); all 5 audit actions confirmed written. A separate 19-check regression script confirmed Analytics, Search, Saved Views, the full Kaizen lifecycle (submit→review→approve→implement→business-impact→reward cascade), Chunk 3A's own Preview/History endpoints, Admin, Notifications, and Leaderboard are all unaffected. `npm run typecheck`/`lint`/`build` clean in both repos (the one pre-existing `kaizen-api` lint error is untouched and unrelated; one new frontend lint error — an unescaped apostrophe in the Download Center's copy — fixed during this chunk). Could not verify the interactive click-through flow in a real browser — no browser automation tool available in this environment (same disclosed limitation as every prior milestone).

Not built, by design (no further chunk boundary — these are genuine, disclosed gaps): real email delivery for scheduled reports (in-app notification only; no email infrastructure exists in this codebase); cursor-based chunked fetching beyond the 10,000-row export cap (a single bounded, indexed Prisma query is used instead — see Technical Debt); true multipart/streamed upload all the way to storage (the generated file is streamed from the database in batches during row collection, but materializes as one in-memory Buffer before the final Cloudinary upload or DB write — reasonable for realistic report sizes, documented as a limitation for anything far larger).

### Gamification
Status: Complete for Milestone 9 MVP scope (points, achievements, leaderboard, rewards) — Monthly Challenges/XP/Levels/Streaks explicitly excluded per `01_DATABASE_SCHEMA.md`'s own MVP note
Progress: ~65%
Notes: Backend (`src/modules/gamification/`): `awardPointsAndRecompute` (writes a `points_ledger` row + updates `user_gamification`'s cached `totalPoints`/stat counter in one transaction, then recomputes the acting user's MONTHLY/COMPANY and MONTHLY/DEPARTMENT rank), `checkAndAwardAchievements` (evaluates every active achievement's `criteria` JSON against the user's current stats, awards any newly-qualifying ones — points, `user_achievements` row, `ACHIEVEMENT_UNLOCKED` notification), `recomputeLeaderboard`/`getLeaderboard` (time-windowed sums over `points_ledger.created_at` for MONTHLY/QUARTERLY/YEARLY, no lower bound for ALL_TIME — a genuine per-period computation, not the same all-time total reused 4 times), `getAchievements`/`getUserAchievements`/`getUserPoints`, and `getPointsSetting` (reads `platform_settings.points.*`, the single source of truth for point values — never hardcoded at a call site). New `src/modules/rewards/` (`RewardService.issue` — Super-Admin-callable manual retry; `autoIssue` — the real path, called directly from the `business_impact.recorded` event handler with no HTTP/auth involved, matching the API spec's "Auth: System (internal) or Super Admin"; `getHistory`). Routes: `GET /api/v1/leaderboard`, `GET /api/v1/achievements` (new `gamification.routes.ts`, mounted at the v1 **root**, not `/gamification`, to land on the API spec's exact unprefixed paths), `GET /api/v1/users/:id/{achievements,points,rewards}` (new `users.routes.ts` — only these 3 gamification-adjacent sub-routes; admin user-management CRUD is untouched, still Admin Panel scope), `POST /api/v1/kaizens/:id/rewards/issue` (added to `kaizens.routes.ts`, `requireRole("SUPER_ADMIN")`). `src/events/handlers/index.ts` (a 2-line no-op comment since Milestone 6) now subscribes to all 7 domain events `WorkflowService`/`KaizenService.submit` already emit: `kaizen.submitted`→+10pts/notify dept manager, `kaizen.approved`→+50pts/notify submitter, `kaizen.rejected`/`kaizen.needs_changes`→notify submitter only, `implementation.completed`→+100pts/notify submitter, `business_impact.recorded`→auto-issues the reward (no separate notification type exists for this event on its own — see Known Issues), `reward.issued`→+150pts/notify submitter. `prisma/seed.ts` now seeds the 10 MVP achievements (with `criteria` JSON matching `GamificationService.meetsCriteria`'s switch) and the 8 MVP `platform_settings` keys.

Frontend (`features/gamification/`): new `/leaderboard` page — period (Monthly/Quarterly/Yearly/All Time) + scope (Company/My Department) toggles via the existing dependency-free native-`<select>`-based `Select` component (no new Radix package), a ranked list highlighting the current user's own row, and an achievements grid below it (earned vs. locked, rarity badge, points value or earned date) fed by `GET /achievements` + `GET /users/:id/achievements`; a `Trophy`-icon "Leaderboard" nav item added to `BASE_FOUNDATION_NAV` (visible to every role, unlike "Review"/"Implementation" which stay role-conditional) since the API spec's leaderboard/achievements endpoints are "Auth: Required" for everyone, not scoped to managers.

**A real bug found and fixed during this milestone's own live-DB verification, not assumed upfront**: `GamificationService`'s rank-sync step originally recomputed the company leaderboard and then updated **every** ranked user's `currentRank` inside one `$transaction`, on every single points-awarding event — an O(N) write per action that has no reason to scale with total headcount just to keep one cached display field fresh for N-1 users who didn't do anything. Verification's own test cleanup (hard-deleting a just-created test user) raced against this still-in-flight bulk update and threw `P2025`, surfacing the design flaw. Fixed by updating only the triggering user's own `currentRank` (O(1)) — `getLeaderboard` never reads this cached field anyway (it always recomputes fresh on every read), so the only consumer of any resulting staleness is each user's own `/me` display, which self-corrects the next time *that* user's own event fires.

**Known MVP limitation, documented rather than silently guarded against**: `TOP_CONTRIBUTOR` ("Top 10 monthly leaderboard") and `DEPARTMENT_HERO` ("highest points in department monthly") are implemented as literal rank checks (`companyRank <= 10`, `departmentRank === 1`). In a department or company slice with very few active point-earners — most visibly a brand-new department, or early in the platform's life before many employees have participated — the first (or only) person to earn any points is trivially "top" or "highest" by default, which the achievement's literal wording technically satisfies but its competitive intent doesn't really intend. No minimum-contender threshold was added, since none is documented and the criteria as built match the spec exactly; flagged here for a future call on whether real-world department sizes make this a non-issue in practice (verified against a large existing company roster, not just the isolated test department used for verification).

**Fixed post-Milestone-9 (2026-07-10) — the "notify department manager on submit" side effect never actually fired in real usage.** `handleKaizenSubmitted` (`src/events/handlers/index.ts`) resolved "the department's manager" via `Department.managerId`, a schema field that no code path in this entire application ever writes (confirmed by grepping the whole backend — the only references were the 3 read-only lines in this one handler). Every other place that needs "the department's manager" — `ReviewService.getQueue`'s queue scoping, `ReviewService.assertCanManage`'s review-action authorization — instead models it as "a `User` with `role: DEPARTMENT_MANAGER` whose own `departmentId` matches," never via a FK on `Department`. Because `Department.managerId` is always `null` in real data, the `if (department?.managerId ...)` guard was always false and the notification was silently never created — not a delivery or frontend bug, the row never existed. This is exactly why the Kaizen still appeared correctly in the Review queue and could still be approved (neither of those paths touches `Department.managerId`), and why every other notification (approval, implementation, reward) worked normally — they all target `kaizen.submitterId`, a field that's always populated, and never needed to resolve "who manages this department" at all. Root cause traced by a user-reported manual-testing bug; my own Milestone 9 verification script had manually seeded `Department.managerId` in its own test setup (a step no real code path performs), which is why the automated 44-check suite passed while this was broken in practice — a real gap in that verification, not a false positive in the app. Fixed with a single-file change: `handleKaizenSubmitted` now resolves the manager via `prisma.user.findFirst({ where: { departmentId, role: "DEPARTMENT_MANAGER", isActive: true } })`, matching `review.service.ts`'s established pattern exactly. Re-verified against the live DB with a department whose `managerId` was deliberately left `null` (the real-world condition) — the manager now receives the notification, visible via both `GET /notifications` and `GET /notifications/unread-count`.

Not built, by design (MVP scope exclusion, not a gap): XP/Level system, Monthly Challenges, Streak System, Trophy Cabinet/Celebration Animations UI flourishes from `02_PRODUCT_REQUIREMENTS.md`'s GAMIFY-001 — `01_DATABASE_SCHEMA.md` explicitly scopes these out ("Basic gamification only. No monthly challenges, XP levels, or streaks in MVP."), and the schema itself has no tables for them. Real cron scheduling for `leaderboard-refresh.job.ts` — implemented as a plain `setInterval` (5-minute period, matching the doc's "every 5 minutes (cron)" note) rather than installing a scheduler library; every read path recomputes fresh regardless, so this job is a best-effort freshness nudge, not a correctness dependency.

### Administration Portal
Status: Complete for Phase 2 Module 1 scope (User/Role/Department/Category Management, Platform Settings) — Branch Management explicitly out of scope this round; scoring-parameter config, announcements, and audit-log viewer not part of this module's requested scope either
Progress: ~55%
Notes: `ADMIN-001` has no detailed body spec anywhere in `02_PRODUCT_REQUIREMENTS.md` (a pre-existing documentation gap, noted since Milestone 1's Known Issues) — this module was built against `02_API_SPECIFICATION.md`'s concrete "Users (Admin)"/"Departments"/"Categories"/"Admin" sections instead, per this engagement's established "specific API spec line governs over an absent/vague product doc section" precedent.

Backend: new `src/modules/admin/` — `AdminUserService` (`list` with search/role/department/isActive filters, `getById`, `create`, `update`, `remove`) backing the API spec's documented `GET/POST /api/v1/users`, `GET/PATCH/DELETE /api/v1/users/:id`. `create` does not duplicate Milestone 2's user-sync logic: it calls the real Clerk Backend API (`clerkClient.users.createUser`, `skipPasswordRequirement: true` — the created user completes their own sign-in via Clerk's normal forgot-password flow, which functions as "invite via Clerk" per the spec's own wording) and then reuses `authService.syncUser` (unchanged) for the actual local-row creation, adding only `departmentId` afterward since that field is outside `syncUser`'s existing scope. `remove` blocks a Super Admin from deactivating their own account. `department.service.ts`/`category.service.ts` (both Milestone 4 read-only stubs) gained `create`/`update`(/`remove` for departments) — Department `create`/`update` validate `managerId` against a real, active user; both services convert a raw Prisma unique-constraint violation (name/code/slug) into a clean `409` rather than a `500`. New `src/modules/admin/platform-settings.service.ts` backs `GET/PATCH /api/v1/admin/settings` — the same `platform_settings` table Milestone 9 seeded with 8 `points.*`/`upload.*`/`pagination.*` keys, now editable for the first time; `PATCH` rejects unknown keys rather than silently creating new settings. A new shared `src/utils/slugify.ts` replaces `prisma/seed.ts`'s previously-private local `slugify` function (used identically by `create`d categories now) — a small, deliberate de-duplication, not a refactor of unrelated logic. Every write is Super-Admin-gated (`requireRole("SUPER_ADMIN")`; `GET /users/:id` allows HR/CMD read-only via the same role hierarchy) and audit-logged (`admin.user.{create,update,delete}`, `admin.department.{create,update,delete}`, `admin.category.{create,update}`, `admin.settings.update`).

Frontend: new `features/admin/` (previously empty `.gitkeep` scaffolding) — a dependency-free `AdminTable` (`components/data-table/*` were literal `return null;` stubs planned around `@tanstack/react-table`, which isn't installed; this is a minimal, working replacement scoped to what the Admin Portal actually needs, no sorting/column-visibility toolbar), 4 list views (Users, Departments, Categories, Settings) each with a create/edit `AlertDialog`-based form (reused as a generic modal container — no dedicated Dialog primitive exists in this design system, only `alert-dialog`/`label`/`slot`/`select`, so this avoids a new Radix dependency), and a landing `/admin` overview page linking to each. New `/admin`, `/admin/users`, `/admin/departments`, `/admin/categories`, `/admin/settings` routes, each wrapped in a new `AdminGuard` client component (checks `lib/permissions.ts`'s existing `canAccessAdmin`, unused until now) so a non-Super-Admin sees a clean "Access restricted" state instead of a page full of controls that would all 403. A new role-gated "Administration" sidebar/mobile-nav item (Super Admin only) added to the shared `foundation-nav.ts`.

**Two real bugs found and fixed during this module's own live-DB verification, not assumed upfront:**
1. `PlatformSettingsService.update`'s audit-log write used a comma-joined string of setting *keys* (e.g. `"points.kaizen_submitted"`) as `entityId` — but `audit_logs.entity_id` is a genuine Postgres `UUID` column, so any settings PATCH crashed with a `P2023` "invalid UUID" error before this module's own verification caught it. Fixed to write one audit row per setting, each keyed by that setting's own real `platform_settings.id` UUID — more correct than the original single-combined-row design anyway.
2. (Reported by the user via manual testing between Milestone 9 and this module, fixed as a standalone patch — see the Notifications & Gamification module entry's "Fixed post-Milestone-9" note above.) `kaizen.submitted`'s manager notification silently never fired because it resolved "the department's manager" via `Department.managerId`, a field nothing in the app ever wrote. This module's own `POST/PATCH /departments` finally gives that field a real write path (`managerId` in the create/update body) — but the notification lookup itself was fixed to match the established `User.role === DEPARTMENT_MANAGER && departmentId` pattern already used everywhere else (`review.service.ts`), not to start depending on `Department.managerId` being populated.

**A note on data-model scope, not a bug**: `Department.managerId` is now genuinely settable (via this module) and does get validated (must reference a real, active user) — but nothing in the app *reads* it for authorization or notification purposes; those all independently derive "the department's manager" from `role + departmentId`. `managerId` is effectively a display/record-keeping field for now. Revisit if a future module wants a department to have exactly one canonical manager distinct from "any Department Manager assigned to this department."

Verified: 32/32 checks against the live database (and, for user creation specifically, the real Clerk Backend API — a disposable `@example.com` test account was created and deleted via `clerkClient.users.deleteUser` in the verification script's cleanup, leaving no orphaned Clerk account) — RBAC across Employee/Department Manager/HR/Super Admin for every new endpoint; user create (real Clerk account + local sync + department assignment), duplicate-email conflict, update (role/department), self-delete guard, soft-delete, and all 3 actions' audit trail; department create (with manager), duplicate-name conflict, invalid-manager rejection, soft-delete; category create (slug generation), duplicate-name conflict, update/deactivate, unrestricted read; settings list (8 seeded keys), update (single and multi-key, each producing its own correctly-UUID'd audit row), unknown-key rejection. `npm run typecheck`/`lint`/`build` clean in both repos (the one pre-existing `kaizen-api` lint error is untouched and unrelated). No Prisma schema changes — every field this module manages already existed. Could not verify the interactive click-through flow in a real browser — no browser automation tool available in this environment (same disclosed limitation as every prior milestone).

Not built, by design (explicitly out of scope for this module — see Known Issues): Branch Management (no schema/spec exists for it anywhere; deferred pending a design decision on what a "Branch" represents in this app); scoring-parameter admin editing, audit-log viewer, and announcements management (all documented under the API spec's same "Admin" section, but not part of this module's requested scope — User/Role/Department/Category Management, Platform Settings — left for a future module).

### Knowledge Base
Status: Not Started
Progress: 0%
Notes: `features/knowledge-base/*` (frontend) is empty; backend `knowledge-base` module is empty. No `knowledge_base_entries` table, no publish flow, no search.

## Known Issues
- **Schema model count corrected**: earlier status entries said "26 models" for `prisma/schema.prisma` — the accurate count is **28** (organization 2 + users/auth 2 + kaizen_core 6 + review_and_scoring 4 + implementation 2 + business_impact 1 + gamification_mvp 5 + knowledge_base 1 + notifications 1 + platform 2 + audit_and_timeline 2). Verified against both the migration SQL (28 `CREATE TABLE`) and the live database catalog.
- **Fixed this milestone — sign-in/sign-up routing bug**: `app/(auth)/sign-in/page.tsx` and `sign-up/page.tsx` were plain (non-catch-all) routes while Clerk's `<SignIn>`/`<SignUp>` were configured with `routing="path"`, which requires a catch-all segment (`[[...sign-in]]`) so Clerk can push internal sub-routes (password reset, verification steps) without 404ing. Converted both to catch-all routes; `/forgot-password` (which mounted a second, disconnected `<SignIn>` instance that could never reach the reset form) now redirects into `/sign-in`'s own built-in forgot-password flow.
- **Documentation gap**: `docs/product/02_PRODUCT_REQUIREMENTS.md`'s own module list at the top (`DASH-002`, `KAIZEN-002`, `REVIEW-002`, `NOTIFY-001`, `LEADERBOARD-001`, `BADGE-001`, `PROFILE-001`, `ADMIN-001`, `SEARCH-001`) is never actually detailed in the document body — the body instead defines `SCORE-001`, `GAMIFY-001`, `WORKFLOW-001` (undeclared in the ToC). Admin Dashboard, My Ideas, Notifications, Profile, Settings, and Knowledge Base modules have **no detailed spec at all** to build or audit against.
- **Conflicting lifecycle definitions across docs**: `REVIEW-001`'s status enum, `IMPLEMENT-001`'s lifecycle, and `WORKFLOW-001`'s state machine each order/name stages differently (e.g. "Business Impact Review" vs. "Verified", presence/absence of "Rejected", "Submitted Again"). The Prisma-schema doc's `KaizenStatus` enum (12 values) is the most concrete version and should likely be treated as canonical when implementing.
- **Verification outcome mismatch**: `IMPLEMENT-001` defines Pending/Verified/Rejected; `WORKFLOW-001` adds an undefined "Needs Rework" outcome with no enum home.
- **Repo topology contradiction**: `00_ARCHITECTURE.md` diagrams a single unified tree; `02_FOLDER_STRUCTURE.md` explicitly mandates two separate repos (matches actual repo layout on disk). Treat the two-repo model as authoritative.
- **Color-token reuse**: Design system assigns "Red" to both Primary (CTAs) and Danger (errors/rejected), contradicting its own single-accent-per-component rule.
- **Dead/divergent code in kaizen-web**: `constants/navigation.ts` defines full role-based nav trees, but `app-sidebar.tsx`/`mobile-nav.tsx` use a separate hardcoded single-item list instead — the richer constant is currently unused.
- **Stray zero-byte files** at `kaizen-web` repo root (`agent.txt`, `agent_err.txt`, `login.txt`, `login_err.txt`, `out.txt`, `whoami.txt`, `whoami_err.txt`, `err.txt`) — leftover shell-redirection artifacts, not part of the app; harmless but worth cleaning up.
- **`kaizen-web` has its own nested `.git`** — confirm intended repo boundary before any tooling assumes a single top-level git repo across the whole workspace.
- **New — bootstrap "General" department**: `kaizens.department_id` is `NOT NULL`, but real department management is Admin Panel scope (not built). Without *some* department to assign, no Kaizen could ever be submitted, so Milestone 4 seeded a single "General"/`GEN` department as a pragmatic unblocker — not something any doc asked for. Replace/supersede this once the Admin Panel exists.
- **New — Department field simplified vs. doc**: AUTH's Step 2 spec (and KAIZEN-001) says Department should be "Editable only by privileged users" (auto-filled from profile, employees can't change it). Milestone 4 preselects the user's department but leaves the dropdown editable for everyone — there's only one department to pick from right now anyway (see bullet above), so the restriction would have no observable effect yet. Revisit once real, multiple departments exist.
- **New — Milestone 5's status filter uses the full 12-value `KaizenStatus` enum, not the milestone brief's literal 7-item list** (All/Draft/Submitted/Under Review/Approved/Rejected/Implemented). The brief's list doesn't map cleanly onto the real schema — "Implemented" isn't an actual status value (the closest are `IMPLEMENTATION_IN_PROGRESS`/`IMPLEMENTATION_COMPLETED`) — so the filter reuses the existing `KAIZEN_STATUS_LABELS` constant (single source of truth, already used elsewhere) instead of introducing a second, incomplete status vocabulary. Same reasoning as Milestone 4's step-count reconciliation.
- **New — transient database connectivity during Milestone 5**: mid-session, the Neon Postgres endpoint became briefly unreachable (`P1001: Can't reach database server`) for about 30–40 seconds, affecting even read-only commands like `prisma migrate status` — unrelated to any code change. Retrying after a short wait succeeded; this is consistent with a Neon compute cold-start/autoscale-from-zero delay, not a bug. Noted here in case it recurs and looks alarming in a future session.
- **New — the same transient Neon cold-start delay recurred during Milestone 6's verification script** (`P1001` on the first `prisma.department.upsert()` call); resolved the same way (retry after ~15s). Consistent with the Milestone 5 note above, not a new issue.
- **New — Milestone 6's Approve/Reject actions intentionally skip the API spec's documented precondition** ("Evaluation submitted with recommendation: APPROVE/REJECT") — enforcing it needs the Scoring Engine's `Evaluation` model, which doesn't exist yet (0% progress, see the Scoring module entry). A Department Manager can currently approve or reject directly from `UNDER_REVIEW` with only an optional free-text `notes` field. Revisit once the Scoring Engine milestone ships.
- **New — `PATCH /kaizens/:id/priority` was not implemented**, even though the API spec lists it under the same "Review" section as the actions this milestone built. It doesn't correspond to a status transition (priority is unrelated to `KaizenStatus`) and wasn't in the milestone brief's explicit action list (Start Review/Approve/Reject/Request Changes) or its reviewer-detail-page spec (which lists fields the reviewer should *see*, not edit) — deferred rather than guessed at.
- **New — "Begin Evaluation," mentioned as an example review action in the milestone brief, was not implemented.** It doesn't correspond to any `KaizenStatus` transition on its own — creating/editing an `Evaluation` record is Scoring Engine territory (a separate, not-yet-built milestone), not a workflow-status change. Building a real "Begin Evaluation" action means building the Scoring Engine first.
- **New — comment replies (`parentId`) are backend-only.** The API accepts and validates `parentId` exactly per the documented request shape, and `ReviewComment`'s self-relation supports threading, but the Milestone 6 frontend always posts top-level comments (`parentId: null`) — no "reply to this comment" UI exists yet. Not required by the milestone brief's comments section, which only asks to "implement reviewer comments... persist them... display them," not build threading UI.
- **New — "review dashboard" (mentioned in the Milestone 6 frontend requirements) was interpreted as the review queue page itself**, not a separate reviewer-facing stats/KPI page — the milestone brief gives a full, separate spec for the review *queue* and *detail* page, but no equivalent spec for reviewer stats (unlike the real company Dashboard, built in Milestone 3 with its own detailed brief). No aggregate-stats endpoint was built as a result.
- **New (Milestone 7) — SCORE-001's "User Access" list (Department Manager, HR, CMD, Super Admin) is broader than the API spec's per-endpoint Auth lines** ("Department Manager (same department)" for `PUT`/evaluate, unqualified "Department Manager" for `GET`/submit) — resolved the same way Milestone 6 resolved the identical conflict for Approve/Reject/Start Review: the specific per-endpoint API spec line governs, not the module-level product-doc list. HR/CMD/Super Admin can view a Kaizen and its finalized score but cannot create or submit an evaluation.
- **New (Milestone 7) — the Approve/Reject evaluation precondition checks the *acting* manager's own evaluation** (`(kaizenId, reviewerId)`), not "any evaluation on this Kaizen." In the current one-department-manager-per-department model this is the only evaluation that could exist anyway, but it's worth knowing if a future milestone ever allows multiple reviewers per Kaizen.
- **Fixed this milestone — implementation owner couldn't view the Kaizen they were assigned to.** `AssignImplementationSchema.assignedDepartmentId` lets a manager assign implementation work to a department other than the Kaizen's own — but `KaizenService.assertCanView` (Milestone 4/5) only recognized submitter / same-department-manager / HR-CMD-SuperAdmin as valid viewers, so a cross-department owner got `403 "You cannot view this Kaizen."` on the very page hosting their own progress-update controls. Fixed with one additive branch (`kaizen.assignedOwnerId === requester.id`); every previously-valid viewer is unaffected (verified with a dedicated 3-check regression script). Found during this milestone's own live-DB verification, not assumed in advance — a good example of why "verify against the real database" catches things a design review wouldn't.
- **New (Milestone 8) — `GET /api/v1/implementations` is the first list/queue endpoint in this codebase that grants Employees read-only access** (every prior queue — Review, and by extension the Scoring endpoints — blocks Employees outright). This is per the API spec's own line for this specific endpoint ("Employee (own Kaizens read-only)"), not an inconsistency — the frontend nav link is still hidden from Employees by choice (see the Implementation & Business Impact module notes), since they already see the same data via their own My Ideas detail page.
- **New (Milestone 8) — `verify` doesn't have a dedicated `TimelineEventType` enum value**, unlike every other action this milestone (or Milestones 6/7) added. It reuses the generic `STATUS_CHANGED` value with `{verificationStatus, notes}` in the event's `metadata` column rather than leaving verification unlogged or inventing a new enum value outside the documented schema.
- **Fixed this milestone (Milestone 9) — O(N) writes per points-award event.** `GamificationService`'s leaderboard rank-sync originally updated every ranked company user's `currentRank` in one transaction on every single points award, regardless of who earned the points. Found via a P2025 crash during verification (a test's own cleanup hard-deleted a user mid-flight, racing the bulk update) — fixed to update only the acting user's own rank (O(1)); see the Gamification module entry for the full explanation.
- **New (Milestone 9) — `implementation.completed` awards points despite the API spec's Domain Events table listing only `NotificationService`, not `GamificationService`, as its subscriber.** `01_DATABASE_SCHEMA.md`'s MVP point-values table lists a concrete "Implementation completed = 100" award with no other documented trigger point for it anywhere — treated as the more specific/authoritative source for *when* points are earned, and the Domain Events table's subscriber list as an incomplete cross-reference rather than a deliberate exclusion. `GamificationService` subscribes to this event in addition to `NotificationService`.
- **New (Milestone 9) — `business_impact.recorded` has no notification of its own.** The `NotificationType` enum has no "business impact recorded" value — only `REWARD_ISSUED`. Resolved by treating `business_impact.recorded`'s side effects as *entirely* the automatic reward-issuance cascade it triggers (matching `business-impact.service.ts`'s own Milestone 8 doc comment, "Side effects: Triggers reward issuance automatically") — the `reward.issued` event's own notification is what satisfies the Domain Events table's "business_impact.recorded → NotificationService" line, not a second, separate notification.
- **New (Milestone 9) — "Business impact verified" (the MVP point-values table's wording) is treated as synonymous with "business impact recorded" (the actual domain event/endpoint name).** No separate impact-verification step or status exists anywhere in the schema (unlike `Implementation.verificationStatus`, which does have one) — the 150-point award fires when the reward is auto-issued off `business_impact.recorded`, which is the only plausible trigger point the rest of the schema supports.
- **New (Milestone 9) — the Leaderboard-refresh "cron" (`01_DATABASE_SCHEMA.md`: "refreshed on reward events and every 5 minutes (cron)") is a plain `setInterval`, not a real scheduler.** No cron/job-queue library is installed in this codebase; every leaderboard read recomputes fresh regardless (see Gamification module notes), so this is a best-effort background nudge, not a correctness dependency. Revisit if a real job runner (e.g. for retryable notification delivery) is ever needed for other reasons.
- **New (Administration Portal) — "Branch Management" has no existing schema, API spec, or product-doc definition anywhere in this codebase.** Greped both repos and every doc; the only mention anywhere is a one-line "Future Scalability: Multiple Branches" aspiration in `02_PRODUCT_REQUIREMENTS.md`, with no field/entity definition. The user explicitly chose to skip it this round (asked directly, given the size of the decision — a new data model — rather than guessing) pending a design decision on what a "Branch" represents for this specific company (e.g. is Muliya Gold & Jewellers single-location today, or does "Branch" mean something closer to "Department" already modeled?). Not built.
- **Fixed this milestone (Administration Portal) — audit-logging a bulk settings update with a non-UUID `entityId` crashed the endpoint.** `PlatformSettingsService.update`'s first draft wrote a single audit row per PATCH using a comma-joined string of setting *keys* as `entityId` — but `audit_logs.entity_id` is a genuine Postgres `UUID` column, so this failed with `P2023` for any PATCH (caught by this module's own live-DB verification, not assumed upfront). Fixed to write one audit row per setting, each keyed by that setting's own real `platform_settings.id`.
- **New (Milestone 11 Chunk 2) — global search's "Users"/"Departments"/"Categories" groups are Super-Admin-only**, narrower than the milestone brief's own example output (which shows a "Departments" group with no stated role restriction). Every page those results could link to (`/admin/*`) is itself Super-Admin-gated, so showing the group to a broader audience would mean returning results the viewer can never open — resolved in favor of "never link somewhere the viewer can't go" over matching the example literally.
- **New (Milestone 11 Chunk 2) — the Review Queue's "Employee"/"Assigned To" advanced filters only populate once a single department is known** (company-wide roles must pick a department first; a Department Manager's own department is used automatically). No company-wide "list every employee" endpoint exists — building one wasn't judged worth it for two filter dropdowns when the existing per-department picker (`GET /departments/:id/users`, Milestone 8) already covers the realistic case.
- **New (Milestone 11 Chunk 2) — "Search Suggestions" (Part 6) doesn't include "popular searches."** No query-popularity tracking exists anywhere in this codebase; adding a table purely to rank search terms was judged disproportionate to a suggestions-polish feature. Recent searches and live matching results (Kaizens/Users/Departments/Categories/Pages) are real; "popular" is simply not a suggestion category yet.
- **New (Milestone 11 Chunk 3A) — `AnalyticsService.getStatusCounts()`'s `implementationComplete`/`implementationPending` fields are an exact-current-status pipeline-funnel snapshot, not a cumulative "ever reached this stage" count.** A Kaizen that completed its implementation and then progressed further (business impact recorded, reward issued, archived) no longer matches `status === "IMPLEMENTATION_COMPLETED"` and drops out of that count. This is correct and intentional for the Analytics Dashboard's live funnel view (Chunk 1, unchanged) but was a real bug for the new Implementation Report, whose subject is cumulative completion — fixed locally in `buildImplementationReport` (see the Reports module entry) rather than in `AnalyticsService`, to avoid changing the Analytics Dashboard's own already-shipped semantics. Any *future* consumer of `getStatusCounts()` that wants "ever completed implementation" rather than "currently sitting in that exact stage" needs the same `IMPLEMENTATION_COMPLETE_OR_LATER`-style status list, not the raw field.
- **New (Milestone 11 Chunk 3B) — no real Cloudinary account is configured in this environment.** `kaizen-api/.env` has `CLOUDINARY_CLOUD_NAME`/`CLOUDINARY_API_KEY`/`CLOUDINARY_API_SECRET` present as keys but with empty values — `getCloudinaryConfig()` correctly treats these as "not configured" (same falsy-empty-string check as always) and returns `null`. Asked the user directly before building anything (this blocks `downloadUrl` — central to almost every part of the Export Engine — from being verifiable at all), and the user chose a same-origin fallback: the real Cloudinary SDK integration is fully built and will activate automatically the moment real credentials are added (`uploadReportFile`/`deleteReportFile` in `src/lib/cloudinary-client.ts`), but until then, generated files are stored directly in Postgres (`ReportExport.fileData Bytes?`) and served from a same-origin download endpoint instead. Fully real, fully live-verified (35/35 checks including an independently-confirmed real `%PDF-` file header) — just not Cloudinary-hosted yet. Revisit once real credentials exist; no code change will be needed, only the `.env` values.
- **New (Milestone 11 Chunk 3B) — Scheduled Reports' "Recipients" (Part 6) are real platform Users notified in-app, not email addresses.** No email-sending infrastructure exists anywhere in this codebase (Phase 2's own separate "Email Notifications" module is listed as not started) — building one specifically to satisfy this one field was judged out of this chunk's scope. `ReportSchedule.recipientIds` stores real, validated (`isActive`) User UUIDs, and each scheduled run creates a genuine `REPORT_READY` notification per recipient (verified live) — "Recipients" is satisfied through the platform's existing, real notification system, not a fabricated email send.
- **New (Milestone 11 Chunk 3B) — the Schedule recipient picker only populates once a department is chosen**, the same documented scope trim as the Review Queue's "Employee"/"Assigned To" filters (Chunk 2) — reuses `useDepartmentUsers` rather than a new company-wide "list every employee" endpoint.

## Technical Debt
- `prisma/schema.prisma` cannot express three documented CHECK constraints (`kaizen_5why.level` 1–5, `evaluation_scores.score` 0–10, `implementations.progress_percent` 0–100) — Prisma's schema DSL has no CHECK support. These must be added via a hand-edited raw-SQL migration; until then those bounds are enforced only at the application layer, not the database (Milestone 7 added the actual application-layer enforcement for `evaluation_scores.score` — Zod's `.min(0).max(10)` — the missing DB-level CHECK itself is unchanged, still pending that future migration).
- `knowledge_base_entries.search_vector` is modeled as `Unsupported("tsvector")` (excluded from the Prisma Client) — the actual generated column, its update trigger, and its GIN index still need to be added by hand-written SQL in a dedicated migration (the doc itself plans this as a separate `002_add_search_vector` migration, not part of the initial schema).
- `services/api/index.ts` (`export const apiServices = {}`) and `lib/api-client.ts`/`lib/clerk.ts` are fully built infra with zero callers — will need real service modules per feature domain rather than growing this file ad hoc.
- No test framework installed in either repo — testing strategy needs to be decided before the module build-out grows large enough that retrofitting tests is painful. This is now more pressing: the auth module (webhook verification, sync logic, JIT fallback) is exactly the kind of logic that benefits most from regression tests, and it currently has none.
- In-memory rate limiter is not distributed-safe (resets per instance) — fine for single-instance dev, needs revisiting before multi-instance deployment.
- **New**: `AuthService.resolveOrSyncUser`'s just-in-time fallback calls the Clerk Backend API on every request from a user the local DB doesn't yet know about (not just once) until the webhook or JIT sync succeeds in writing the row — fine for the expected case (webhook usually beats the user's first API call), but if `CLERK_WEBHOOK_SECRET` is ever left unset in an environment that also has real traffic, every request from new users pays an extra Clerk API round-trip indefinitely instead of failing loudly. Worth a startup warning log if the webhook secret is unset in a non-development environment.
- **New**: Repo-wide Prettier drift — `npm run format:check` in `kaizen-web` currently flags 52 pre-existing files (not touched this session) as non-compliant. Not introduced by Milestones 1–2, but worth a dedicated formatting pass before it grows further.
- **New**: 3 pre-existing ESLint errors in `kaizen-api` (`error-handler.ts` unused param, `audit.service.ts` and `workflow.service.ts` empty classes) — confirmed pre-existing (not introduced by Milestones 1–2), left untouched since fixing them is outside Authentication scope and those modules aren't part of this milestone.
- **New**: `components/ui/tooltip.tsx` (Milestone 3) is a hand-rolled CSS-only tooltip (group-hover/group-focus-within), not `@radix-ui/react-tooltip` — deliberate, to avoid an unrequired new dependency (the milestone brief explicitly said not to install packages without asking). Fine for its current use (a static "Coming soon" label), but it has no `Escape`-to-dismiss, no delay/debounce, and — the one real limitation worth knowing — it never triggers on the `QuickActionsCard` buttons specifically, because those buttons use the native `disabled` attribute, which removes them from the Tab order entirely (`group-focus-within` has nothing to focus). Not fixed: these are placeholder buttons slated for replacement once the Kaizen Wizard/Gamification milestones ship real actions, so the proper fix (`aria-disabled` + explicit `tabIndex` instead of `disabled`) was judged not worth the complexity for UI that's temporary by design. Revisit if this Tooltip is reused on a *permanently* disabled-but-explainable control.
- **New**: `KaizenService.submit`'s validation and the status-transition logic live directly in the Kaizen module rather than a shared `WorkflowService` (which doesn't exist — see the module notes above). When the Workflow Engine milestone builds the real state machine, this narrow transition should be migrated into it rather than left as a parallel, inconsistent path.
- **New**: Kaizen Wizard autosave has no conflict handling for two browser tabs editing the same draft — the last PATCH to land wins silently. Unlikely in practice (one user, one wizard session) but worth knowing before this pattern is reused elsewhere.
- **New**: The wizard's draft-recovery `localStorage` snapshot can drift from the server if a PATCH succeeds but `writeStoredDraft` somehow doesn't (e.g. localStorage quota, private browsing) — recovery would then resume slightly stale local values even though the server has newer ones. Low risk (both are written in the same code path immediately after each other) but not transactional across the two.
- **New (Milestone 6)**: `KaizenService.submit`'s `DRAFT`/`NEEDS_CHANGES`→`SUBMITTED` transition (Milestone 4) still does not go through `WorkflowService.transition()`, even though `WorkflowService` is now real (Milestone 6). Migrating it was out of scope for this milestone (refactoring completed Milestone 4 code wasn't requested) but should happen once a milestone touches that code path anyway, so all status transitions eventually funnel through one engine as the API spec intends.
- **New (Milestone 6)**: `WorkflowService`'s transition table (`REVIEW_TRANSITIONS` in `workflow.service.ts`) only covers the two review-workspace edges this milestone needed. It is not yet a general state machine for all 12 `KaizenStatus` values — later milestones (Implementation, Business Impact, Rewards, Archive, Knowledge Base) will need to extend this table rather than build a second, parallel transition mechanism.
- **New (Milestone 6)**: The review queue's default status filter (everything except `DRAFT`) is a judgment call, not something the API spec states explicitly — the spec only says the queue's `status` param is optional and comma-separated, without defining default behavior. Revisit if a future milestone's UX expectations differ (e.g. wanting `APPROVED`/`REJECTED` Kaizens to disappear from the queue by default rather than requiring an explicit filter).
- **New (Milestone 7)**: `ReviewService.approve`/`reject` now query `prisma.evaluation` directly rather than going through `ScoringService` — a small, deliberate cross-module reach matching how `ScoringService` itself reuses `kaizenService.getById` rather than either module wrapping the other in a shared abstraction. Revisit if the scoring/evaluation domain grows enough to warrant a shared read-only accessor.
- **New (Milestone 8)**: `ImplementationService`'s and `BusinessImpactService`'s near-identical `loadKaizen`/`assertCanView` pair (submitter/owner/HR-CMD-SuperAdmin/dept-manager-same-dept) is duplicated verbatim across both new modules rather than shared — consistent with this codebase's established per-module `assertCanX` convention (see the Milestone 7 Scoring note above), but this specific pair is a stronger DRY candidate than most: it's identical in both files, introduced in the same milestone, and is exactly the rule `KaizenService.assertCanView` almost has (see the Known Issues fix above) except for the owner branch. If a third module ever needs the same rule, worth promoting to a shared helper instead of a third copy.
- **New (Milestone 8)**: No real file upload exists for implementation evidence — `POST /kaizens/:id/implementation/attachments` only registers metadata for a file already hosted elsewhere. Same pre-existing gap as Kaizen attachments (Milestone 4); still blocked on Cloudinary SDK + signed-upload endpoint, neither installed.
- **New (Milestone 9)**: Rank-based achievement criteria (`TOP_CONTRIBUTOR`, `DEPARTMENT_HERO`) have no minimum-contender guard — see the Gamification module entry's "Known MVP limitation" note. Worth a design decision once real usage data shows whether this matters in practice.
- **New (Milestone 9)**: `RewardService`/`GamificationService`/`NotificationService` are wired together entirely through `src/events/handlers/index.ts` rather than any of them calling each other directly (except `RewardService`→`GamificationService.getPointsSetting`, a narrow read-only reach for the reward's own point value). This is the correct decoupled shape per the API spec's own domain-event design, but it means the *order* side effects happen in (points → leaderboard recompute → achievement check → notification) is currently encoded once per handler in `events/handlers/index.ts` rather than centralized — if a future milestone adds an 8th domain event, match the existing handlers' order rather than inventing a new one.
- **New (Milestone 9)**: `leaderboard-refresh.job.ts` uses `setInterval` (see Known Issues) — if a real job queue is ever introduced for other reasons, migrate this job to it rather than leaving two different background-execution patterns in the codebase.
- **Fixed this milestone (Milestone 9) — unhandled-rejection risk in `event-bus.ts` (Milestone 4/6 infra, predates this milestone).** `EventBus.emit()` awaited `Promise.all(handlers)` with no per-handler error isolation, and every call site does `void eventBus.emit(...)` with no `.catch()` — a single failing handler (e.g. a transient DB error inside `checkAndAwardAchievements`) would reject `emit()`'s own promise with nothing to catch it, surfacing as a Node `unhandledRejection` that could crash the whole API process over what should be a best-effort side effect. This only became a real risk once `registerEventHandlers()` had actual handlers that touch the database (Milestone 9 is the first milestone to register any). Fixed by catching each handler individually inside `emit()` and logging the failure instead of letting it propagate — a minimal, purely defensive change to the emit loop itself; no call site changed. Still no retry mechanism for a failed notification/points write — a swallowed failure means that one side effect is simply lost, not retried. Worth a real job queue if this proves to matter in practice.
- **New (Milestone 11 Chunk 2)**: `SearchService`'s candidate-fetch caps (2000 Kaizens, 1000 Users) mean fuzzy search runs in-process over a Node-side snapshot rather than a real DB-side search index — fine at Muliya's current scale, but revisit with a genuine Postgres full-text/trigram index (or a dedicated search service) before the corpus is large enough that the caps start truncating real results. The same gap already exists for `knowledge_base_entries.search_vector` (see Technical Debt above) — worth solving both at once if this becomes a real constraint.
- **New (Milestone 11 Chunk 2)**: `SavedView.filters` is stored as opaque JSON and never validated against the target endpoint's own Zod query schema — a saved view built against an older/newer version of a list page's filter set could silently apply stale or unknown params (Zod's `.safeParse` on the receiving endpoint just ignores unrecognized keys, so this fails soft, not hard, but it's still worth knowing).
- **New (Milestone 11 Chunk 3A)**: `ReportService.buildKaizenWhere()` duplicates Chunk 2's filter-field vocabulary (by design — see the Reports module entry) rather than importing a shared filter builder, since no such shared builder exists yet in `review.service.ts`/`implementation.service.ts`/`kaizen.service.ts` (each inlines its own Zod-validated query parsing). If a 4th module ever needs the same filter set, worth promoting to one shared `buildKaizenFilterWhere()` helper instead of a third near-identical copy — same shape of debt already flagged for `ImplementationService`/`BusinessImpactService`'s `assertCanView` pair (Milestone 8 note above).
- **New (Milestone 11 Chunk 3A)**: `ReportGeneration` records only generation metadata (user, reportType, filters, durationMs) — the report's actual computed output (KPIs/charts/table/etc.) is never persisted anywhere. Re-opening a past history entry to view its original output isn't possible; History today is an audit trail of *that a report was generated*, not a way to retrieve *what it said*. ~~Revisit if Chunk 3B's export feature ends up persisting rendered output anyway~~ — resolved, 2026-07-13: it does (`ReportExport`), but the two remain separate, unlinked tables (a `ReportGeneration` history row still can't be "opened" to reach the export that may have followed it) — a future pass could link them if that navigation is ever asked for.
- **New (Milestone 11 Chunk 3B)**: `ReportExport`'s `EXPORT_ROW_CAP` (10,000 rows for CSV/Excel) is served by a single bounded, indexed Prisma `findMany`, not a cursor-based chunked fetch loop — real and safe at Muliya's scale (a hard cap, not an unbounded query), but a report with more real rows than that would silently truncate rather than paginate through the rest. The generated file itself also materializes as one in-memory `Buffer` before final storage/upload (ExcelJS's own streaming `WorkbookWriter` is used for the row-writing phase, and PapaParse's CSV output is a single string) — genuinely fine for realistic report sizes (KPI/chart/table exports, not raw bulk-data dumps), but revisit with true chunked/multipart upload if reports ever need to scale meaningfully beyond that.
- **New (Milestone 11 Chunk 3B)**: `ReportExport.filtersHash` (SHA-256 of `{reportType, format, sortedFilters}`) drives the 10-minute export cache — same class of debt as `SavedView.filters` above: nothing re-validates a cached export's stored filters against the current `generateReportSchema` shape, so a schema change that adds/renames a filter field could theoretically serve a stale cached file for up to 10 minutes before the hash naturally diverges on the next request. Low risk given the short TTL, but worth knowing.
- **New (Milestone 11 Chunk 3B)**: The report-schedule background job (`src/jobs/report-schedule.job.ts`) re-validates a schedule's `reportType` against its owner's role only at schedule creation/edit time, not on every scheduled run — if a Department Manager who owns a `DEPARTMENT` schedule is later promoted or demoted, the next scheduled run still executes under their original, now-possibly-stale role snapshot (refetched fresh from the DB at run time, so role changes ARE picked up, but the reportType itself isn't re-validated against that fresh role every run). Normal behavior for a background job bound to its original authorization grant; revisit only if role changes need to immediately invalidate existing schedules.

## Pending Decisions
- ~~Final-score aggregation method for multi-reviewer evaluations~~ — resolved, 2026-07-09 (Milestone 7): built as documented ("MVP uses Average"). `ScoringService.getScore` averages `totalScore`/`overallRating` across all submitted evaluations for a Kaizen — currently always exactly one (one dept manager per department), but the averaging logic is already correct for a future multi-reviewer scenario. Median/Weighted-Average remain documented future options, not built.
- Which canonical Kaizen lifecycle/state list to implement, given the three conflicting versions across `REVIEW-001`, `IMPLEMENT-001`, and `WORKFLOW-001` docs (recommend adopting the Prisma-schema doc's 12-value `KaizenStatus` enum as ground truth and reconciling the narrative docs to match).
- Charting library choice: docs/architecture specify Recharts, but it isn't installed and `components/charts/*` are inert stubs — confirm before building analytics.
- **New**: AUTH-001 specifies a "Remember Me" checkbox on login; Clerk's prebuilt `<SignIn>` has no equivalent control (session persistence is a Clerk Dashboard-level setting, not a per-login user choice). Decide whether to (a) accept this as a documented deviation, (b) configure Clerk Dashboard session settings to approximate "always remember," or (c) drop the prebuilt widget for a custom `useSignIn()`-based form just to add this one control — (c) is a large scope increase for one checkbox, not recommended.
- **New**: Role assignment currently requires manually editing `public_metadata.role` in the Clerk Dashboard per user. Acceptable as a stand-in until the Admin Panel exists, but confirm this is fine for whoever is onboarding early test users.
- **New**: Whether the single "General" bootstrap department is acceptable to keep using until the Admin Panel ships real department management, or whether a small set of real departments should be seeded sooner (see Known Issues).
- **New**: Confirm the "Business Impact"/"Estimated Savings" → `kaizen_benefits` mapping decision (see Kaizen Wizard module notes) is the right long-term home, versus adding dedicated schema fields later — a schema change now was avoided since it wasn't requested, but this is worth a deliberate call before Review & Scoring or Analytics start reading benefit data and need to distinguish these two from regular expected-benefit entries.
- **New**: `GET /kaizens` currently ignores role entirely and always scopes to the requester — confirm this is fine to leave as-is until the Review Workspace milestone adds the documented role-based scoping (Dept Manager → department, HR/CMD/Super Admin → all), rather than half-building that scoping logic now with no reviewer UI to exercise it.
- **New**: Whether "resume a draft from the My Ideas list" (clicking a DRAFT-status card and landing back in the wizard at the right step) is worth a dedicated follow-up before Review & Scoring, or whether it's fine to leave the list/detail pages read-only for now — today a user can only ever resume the *one* most recent wizard session (via the Milestone 4 localStorage mechanism), not an arbitrary older draft found via browsing the list.
- ~~Whether the Approve/Reject evaluation precondition should be added retroactively~~ — resolved, 2026-07-09 (Milestone 7): yes, added. See the Scoring module entry.
- **New**: Whether `PATCH /kaizens/:id/priority` (documented, not built — see Known Issues) is worth adding as a small standalone follow-up, or should wait and be bundled into a future milestone that touches the review detail page again. Still not built as of Milestone 7 (unrelated to Scoring).

## Next Recommended Task
**Milestone 11 Chunk 4: Knowledge Base & Announcements** — Chunks 1 (Analytics), 2 (Search, Filtering & Saved Views), 3A (Reporting Engine), and 3B (Export Engine & Report Delivery) are all complete and verified. The full Reporting module (Part 7 of "Enterprise Analytics & Knowledge Platform") is now done end-to-end: generate, preview, export (PDF/Excel/CSV), download, schedule, and template. Remaining Milestone 11 scope: Knowledge Base (Parts 8), Announcements (Part 9), Activity Feed/Audit Log Viewer/further Dashboard redesign (Parts 10-12), Archive/Performance/Responsive polish (Parts 13-15). Do not start this or any other chunk until the user explicitly signals Chunk 3B is reviewed and gives the go-ahead.

Required prerequisite for full production readiness of the Export Engine specifically (not blocking further Milestone 11 work): supply real Cloudinary credentials in `kaizen-api/.env` — the SDK integration is already built and will switch over automatically with zero code changes; until then, exported files are served from a same-origin fallback (see the Reports module's Chunk 3B notes and Known Issues).

## Last Updated
2026-07-13 — Milestone 11 Chunk 3B of 6: Enterprise Export Engine & Report Delivery (the rest of Part 7). Backend — new `src/modules/report-exports/` (real PDF via PDFKit with hand-rolled vector charts, real multi-sheet Excel via ExcelJS's streaming writer, real CSV via PapaParse; async PENDING→PROCESSING→COMPLETED/FAILED generation; SHA-256 filter-hash export caching; a same-origin storage fallback since no real Cloudinary account exists in this environment — the real Cloudinary SDK integration is fully built and activates automatically once real credentials are added, see Known Issues), `src/modules/report-schedules/` (Daily/Weekly/Monthly/Quarterly/Yearly, executed by a new `src/jobs/report-schedule.job.ts` background job, notifying recipients in-app via a new `REPORT_READY` notification type), and `src/modules/report-templates/` (favorite/pin/duplicate/recently-opened). `ReportService.generateReport` gained an optional `detailRowLimit` param (default 100, matching the Preview) so exports reuse the exact same engine/RBAC/history/audit call Chunk 3A already built, just with a higher cap for CSV/Excel — no duplicate aggregation query. Three schema additions: `ReportExport`, `ReportSchedule`, `ReportTemplate` models + their enums, and `REPORT_READY` added to `NotificationType` — all additive. Frontend — new `/reports/history` (Download Center), `/reports/schedules`, and `/reports/templates` pages, a `ReportsSubNav` tying all four Reports pages together, export buttons + a "Save as Template" action on the Report Preview, and `REPORT_READY` notification support.

**One real bug found and fixed during this chunk's own live-DB verification**: `POST /reports/exports` returned `201 PENDING` for a Department Manager exporting a report type they're not allowed to generate — the actual permission check only ran inside the async background job afterward (silently flipping the row to `FAILED` rather than rejecting the request). Fixed by moving the RBAC check to run synchronously before the export row is even created, matching how every other endpoint in this app rejects up front rather than "succeeding" and failing invisibly in the background.

Verified: 35/35 checks against the live database — full RBAC matrix across exports/schedules/templates, all 3 export formats completing with real, non-trivial file sizes (a downloaded PDF's bytes independently confirmed to start with the real `%PDF-` header), cross-user download blocked, cache-hit reuse confirmed, full Download Center/Template/Schedule lifecycles, a forced synchronous run of the real schedule-execution job confirming `lastRunAt`/`nextRunAt`/a real `ReportExport` row/a real recipient notification, and all 5 new audit actions confirmed written. A separate 19-check regression script confirmed Analytics, Search, Saved Views, the full Kaizen lifecycle (submit→review→approve→implement→business-impact→reward cascade), Chunk 3A's own Preview/History endpoints, Admin, Notifications, and Leaderboard are all unaffected — one apparent notification-cascade failure during this regression pass turned out to be Neon connection latency (the same class of transient delay already documented several times in this file), not a regression; confirmed by re-running with a longer settle window. `npm run typecheck`/`lint`/`build` clean in both repos (the one pre-existing `kaizen-api` lint error is untouched and unrelated; one new, unrelated frontend `react/no-unescaped-entities` error was fixed during this chunk). No Prisma schema changes beyond the three new models. Awaiting user review before starting Chunk 4 (Knowledge Base & Announcements).

**One real bug found and fixed during this chunk's own live-DB verification**: the Implementation Report's "Completed" KPI initially undercounted any Kaizen that had progressed past `IMPLEMENTATION_COMPLETED` to `BUSINESS_IMPACT_RECORDED`/`REWARD_ISSUED` (the normal case once business impact is recorded and Milestone 9's auto-reward cascade fires) — caught live with a full-lifecycle test Kaizen reporting 0 completed implementations despite one being fully complete and rewarded. Fixed locally in `buildImplementationReport` with a new completed-or-later status list, without touching `AnalyticsService`'s own correct funnel-stage semantics (used unchanged by the Analytics Dashboard) — see the Reports module entry and the new Known Issues note for the full distinction.

Verified: 22/22 checks against the live database — full RBAC matrix (Employee blocked entirely; Department Manager restricted to the `DEPARTMENT` report type only, correctly self-scoped; HR/CMD/Super Admin unrestricted), all 10 report types generating real KPIs/charts/tables/summary/recommendations against a full-lifecycle test Kaizen (submit→evaluate→approve→implement→business-impact→auto-reward), comparison metrics' shape, and Report History's scoping (self-only, Employee still blocked). A separate 11-check regression script confirmed Analytics (incl. the new `avgReviewTimeHours` field), Search, Saved Views, Review Queue, Implementation Queue, Admin, Notifications, and Leaderboard are all unaffected. `npm run typecheck`/`lint`/`build` clean in both repos (the one pre-existing `kaizen-api` lint error is untouched and unrelated). No new npm packages installed. Awaiting user review before starting Chunk 3B (Report Export & Scheduling).

Verified: 44/44 checks against the live database via a temporary script covering the full cascade (submit→+10pts+FIRST_KAIZEN+notify-manager, evaluate+approve→+50pts+notify, assign+complete→+100pts+IMPLEMENTER, business-impact→auto-reward→+150pts+IMPACT_MAKER+REWARD_ISSUED timeline/audit, every GET endpoint's RBAC including self/HR/CMD/SuperAdmin/unrelated-employee, notification mark-read/mark-all-read, and the manual-reward-retry endpoint's Employee-forbidden/already-issued-conflict cases) plus two follow-up regression scripts (8 checks: submitting in a manager-less department doesn't crash and correctly skips the manager notification, existing My Ideas/timeline endpoints still work; 5 checks: `kaizen.rejected`/`kaizen.needs_changes` notifications fire correctly and the Milestone 6 reject/needs-changes paths — now also gated by the Milestone 7 evaluation precondition — still work). `npm run typecheck`/`lint`/`build` clean in both repos (the one pre-existing `kaizen-api` lint error, an unused-but-required Express error-handler parameter, is untouched and unrelated to this milestone). No new npm packages installed; no Prisma schema changes (the schema already fully supported this milestone since Milestone 1). Awaiting approval before starting the next milestone (Analytics & Admin Panel).
