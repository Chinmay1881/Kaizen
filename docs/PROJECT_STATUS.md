# Muliya Kaizen Project Status

## Overall Progress
Overall Completion: ~48%

The documentation set (product, design, engineering) is comprehensive and largely internally consistent (a few contradictions noted below). The codebase in both `kaizen-web` and `kaizen-api` is a **scaffold/foundation checkpoint**: tooling, auth wiring, middleware, and folder structure exist, but almost no business logic, no database schema, and no feature UI has been built yet. Documentation is far ahead of implementation.

## Completed
- **Documentation**: Product overview, roles/permissions, product requirements (with gaps — see Documentation Audit), UI spec, design system, engineering architecture, DB schema spec, API spec, folder structure spec, Cursor coding rules. ~13,000 lines total.
- **kaizen-web tooling**: Next.js 16 + React 19 + TypeScript project bootstrapped; Tailwind 4 + shadcn/ui pattern; ESLint/Prettier scripts; `components.json` configured.
- **kaizen-web auth shell**: Clerk sign-in/sign-up pages wired to real `<SignIn>`/`<SignUp>` components; `middleware.ts` protects all routes except public ones via `clerkMiddleware`/`auth.protect()`.
- **kaizen-web app shell**: Root layout, error/loading/not-found pages, dashboard layout composing `AppSidebar` + `AppHeader` + `MobileNav`, theme provider (next-themes) + TanStack Query provider + Clerk provider composed in `AppProviders`.
- **kaizen-web base UI kit**: 4 shadcn primitives (`button`, `card`, `input`, `label`), feedback components (`empty-state`, `error-state`, `loading-skeleton`, `fade-in`, toast re-export).
- **kaizen-web infra utilities (built, mostly unused so far)**: `lib/api-client.ts` (generic fetch wrapper + `ApiError`), `lib/permissions.ts` (role hierarchy checks), `lib/query-client.ts`, `constants/navigation.ts`, `constants/routes.ts`, `types/enums.ts` (12-state Kaizen status), `hooks/use-debounce.ts`, `utils/format.ts`.
- **kaizen-api tooling**: Express 5 + TypeScript (ESM) + Prisma 6 + Zod 4 bootstrapped; env validation (`config/env.ts`) covers DB/Clerk/CORS/Cloudinary/rate-limit/log-level.
- **kaizen-api middleware**: Real Clerk JWT verification (`@clerk/express`), generic Zod request validator, error handler, 404 handler, in-memory rate limiter, request logger — all functional.
- **kaizen-api constants**: Full role/permission maps and 12-state Kaizen status enum defined ahead of any schema/service using them.
- **kaizen-api infra**: Prisma client singleton, event bus (pub/sub), API error/response/pagination/kaizen-number utilities — all generic and functional, awaiting real models/services to call them.
- **kaizen-api database schema** (Milestone 1, 2026-07-07): Full `prisma/schema.prisma` written per `docs/engineering/01_DATABASE_SCHEMA.md` — 14 enums, 28 models (corrected count; see Known Issues) covering organization, users/auth, kaizen core (incl. 5W1H/5Why/benefits/attachments), review & scoring, implementation, business impact, gamification, knowledge base, notifications, platform settings, and audit/timeline. Migration `001_initial_schema` applied to the real Neon database and independently verified (all 28 tables + 14 enums + 47 FKs present; Prisma Client read/write, relational includes, cascade deletes, and unique constraints all confirmed working end-to-end).
- **Authentication & user sync** (Milestone 2, 2026-07-07): Full auth system, both repos — see the Authentication module entry below for details. Frontend: theme-aware Clerk `<SignIn>`/`<SignUp>` on proper catch-all routes, custom localization copy matching AUTH-001, fixed forgot-password flow, `useCurrentUser()` hook. Backend: Clerk webhook now verifies Svix signatures and syncs `user.created/updated/deleted` into the `users` table (with `UserGamification` row creation), a just-in-time sync fallback for local dev, real `attachUser` middleware populating `req.user` from the DB, and a real `GET/PATCH /api/v1/me`. `middleware/rbac.ts` (`requireRole`) is now actually usable for the first time since `req.user.role` is finally populated.
- **Authenticated Dashboard** (Milestone 3, 2026-07-08): `app/(dashboard)/dashboard/page.tsx` replaced the static "Setup Complete" placeholder with a real dashboard driven by `useCurrentUser()` (→ `GET /api/v1/me`). See the Dashboard module entry below for full detail.
- **Kaizen Submission Wizard** (Milestone 4, 2026-07-08): The first real business feature end-to-end. Backend: new `kaizens` module (create/get/update/delete/submit, ownership + status-gated, kaizen-number generation, submit-time validation, timeline events) plus minimal read-only `departments`/`categories` modules to unblock the wizard's dropdowns, mounted and live. `prisma/seed.ts` now actually seeds data (12 categories with icons/descriptions, one bootstrap "General" department). Frontend: 7-step wizard (`features/kaizen/*`) — Basics, Process, 5 Why, 5W1H, Benefits, Attachments (UI-only), Review & Submit — one React Hook Form + Zod instance spanning all steps, autosave (on step change + 30s interval) against real backend drafts, refresh-safe draft recovery via localStorage, and a real submit flow ending in a success screen. See the Kaizen Wizard module entry below for full detail.
- **My Ideas (Employee Portal)** (Milestone 5, 2026-07-08): Employees can now see, search, filter, sort, and inspect every Kaizen they've submitted — closing the loop the Wizard opened. Backend: `GET /api/v1/kaizens` (list, always scoped to the authenticated user, search/status/category/priority filters, 3-way sort, pagination) and `GET /api/v1/kaizens/:id/timeline`, both mounted and live. Frontend: new `/kaizen` (My Ideas list) and `/kaizen/[id]` (read-only detail) routes, a new sidebar nav item, a reusable `KaizenStatusBadge` backed by 2 new Badge variants (`warning`/`info`, wired to existing design tokens). See the Kaizen Wizard module entry below (My Ideas is tracked as part of it, not a separate module — same underlying `kaizens` domain).
- **Review Workspace** (Milestone 6, 2026-07-09): Department Managers (and, for visibility only, HR/CMD/Super Admin) can now see a role-scoped review queue, open a Kaizen's review detail page, start a review, approve/reject/request changes, and leave/resolve review comments — the workflow no longer dead-ends at `SUBMITTED`. Backend: `WorkflowService.transition()` and `AuditService.record()` (both previously 4-line empty stubs) are now real, matching the API spec's "Workflow Service (Internal)" contract for the transitions this milestone introduces (`SUBMITTED→UNDER_REVIEW`, `UNDER_REVIEW→{APPROVED,REJECTED,NEEDS_CHANGES}`) — validates the transition, updates `kaizens.status`, writes a `timeline_events` row, writes an `audit_logs` row, and emits a domain event (`kaizen.approved`/`kaizen.rejected`/`kaizen.needs_changes`, currently no-ops — no subscribers exist yet). New `reviews` module (`review.service.ts`, reusing `KaizenService.getById`/`KAIZEN_LIST_SELECT`/`toListItem`/pagination helpers rather than duplicating them) backs `GET /api/v1/reviews/queue` (new route file) and 7 new routes nested under the existing `kaizens.routes.ts`: `POST /:id/review/{start,approve,reject,needs-changes}`, `GET/POST /:id/comments`, `PATCH /:id/comments/:commentId/resolve`. Frontend: new `/review` (queue — search/status/department/category/priority filters, sort, pagination, reusing `MyIdeasPagination`/`MyIdeasSkeleton` verbatim) and `/review/[id]` (detail) routes; `KaizenDetailView` (Milestone 5) gained two small optional render-prop slots (`actionSlot`, `extraContent`) so the review detail page composes the existing detail view instead of duplicating its 5W1H/5Why/Benefits/Timeline sections; a role-aware "Review" sidebar/mobile-nav item (only shown to Department Manager and above). See the Review Workspace module entry below for full detail, including what was deliberately deferred (evaluation precondition, priority changes, threaded comment replies).

## In Progress
- **Role assignment UX**: Roles can be set today only via Clerk Dashboard → user → `public_metadata.role`, read by the sync logic on next webhook/JIT sync. This is an intentional stand-in until the Admin Panel (Super Admin UI for role management) exists — not a bug, but not the final experience either.

## Not Started
- Resuming an old DRAFT from the My Ideas list (list/detail are read-only browsing; editing a draft found via the list, rather than the single in-progress wizard session tracked in localStorage, isn't wired up) — see Kaizen Wizard module notes
- Duplicate-draft action (`POST /kaizens/:id/duplicate`) — still not implemented
- Real file uploads for Kaizen attachments (Step 6 is UI-only; no Cloudinary SDK, no `/uploads/sign`, no `POST /kaizens/:id/attachments` wiring) — the My Ideas detail page's Attachments section correctly shows "No attachments" for every Kaizen as a result
- Interactive Scoring Engine (5-parameter evaluation, `evaluations`/`evaluation_scores`) — Review Workspace itself (Milestone 6) is now done; see below
- Implementation Tracking & Business Impact
- Full Workflow Engine state machine covering all 12 `KaizenStatus` values — `WorkflowService.transition()` now exists (Milestone 6) but only validates the review-workspace subset (`SUBMITTED→UNDER_REVIEW→{APPROVED,REJECTED,NEEDS_CHANGES}`); DRAFT/NEEDS_CHANGES→SUBMITTED still lives separately in `KaizenService.submit` (not migrated — see Technical Debt), and later-lifecycle transitions (implementation, business impact, rewards, archive, publish) aren't wired up yet
- Notifications (frontend and backend)
- Analytics & Reporting
- Gamification (Leaderboard, Achievements/Badges, Points ledger)
- Admin Panel (users, departments, categories, scoring parameters, announcements, audit logs, platform settings)
- Knowledge Base
- File uploads / Cloudinary integration (config helper exists, SDK not installed, no upload endpoint)
- Automated tests (zero test files, no test framework installed in either repo)
- Background jobs (`leaderboard-refresh.job.ts` is an empty function)
- 12 of 17 backend route files registered in the barrel file are still unmounted stubs — `meRouter`, `departmentsRouter`, `categoriesRouter`, `kaizensRouter`, and (new, Milestone 6) `reviewsRouter` are wired into `src/routes/index.ts` with real logic (`kaizensRouter` now also has the 7 review-action/comment routes added in Milestone 6, on top of Milestone 5's `GET /` list and `GET /:id/timeline`); `users, scoring, implementations, business-impact, dashboard, analytics, gamification, knowledge-base, notifications, uploads, announcements, admin` remain untouched 4-line stubs (the Clerk webhook itself is real too, but it's mounted separately in `app.ts`, not part of this barrel)
- 9 of 12 frontend `features/*` domains are still empty `.gitkeep` scaffolding — `auth`, `kaizen`, and (new, Milestone 6) `review` have real content now (`review`'s `constants`/`schemas`/`utils` subfolders are still empty — nothing needed them this milestone, since filters/status labels were reused directly from `features/kaizen`)

## Current Sprint
**Milestone 6 (Review Workspace) is code-complete** as of 2026-07-09 — see the Review Workspace module entry for full detail. Remaining loose ends from earlier milestones: migration `002_add_search_vector` still hasn't been written; role assignment still depends on manually editing Clerk Dashboard metadata (no Admin UI yet); scoring parameters/achievements/platform settings still aren't seeded (the last of these now directly blocks the Scoring Engine, the next recommended task). None of these block starting the next milestone.

## Remaining Milestones
*Note: milestone numbering below was set in Milestone 1 and originally sequenced "Kaizen Wizard" as #3 and had no separate Dashboard entry; item 4 originally bundled "Wizard + My Ideas retrieval" as one milestone. Actual delivery order was Dashboard (Milestone 3), Kaizen Wizard (Milestone 4), then My Ideas (Milestone 5) — the latter is item 4's second half, delivered as its own milestone rather than bundled, not a jump to item 5 (Review & Scoring) below. Each is called out explicitly rather than silently renumbered, so this list stays an accurate record of what was asked for, not just what was originally planned.*

1. **Database & Core Domain** — ~~Write the full Prisma schema~~ (done, 2026-07-07) — ~~run initial migration~~ (done, 2026-07-07, verified against the live Neon DB) — ~~seed categories + bootstrap department~~ (done, 2026-07-08); remaining: migration 002 (CHECK constraints + full-text search trigger/index), seed scoring-parameters/achievements/platform-settings once their milestones start.
2. **User Sync & RBAC** — ~~Implement Clerk webhook (signature verification + `user.created/updated/deleted` → `users` table)~~, ~~wire `AuthService`/`requireRole` end-to-end~~, ~~real `GET/PATCH /api/v1/me`~~, ~~theme-aware custom sign-in/sign-up UI~~, ~~Logout Flow with confirmation~~ (all done, 2026-07-07).
3. **Authenticated Dashboard** — ~~Replace the placeholder `/dashboard` with a real profile/stats/quick-actions view fed by `useCurrentUser()`~~ (done, 2026-07-08). Remaining: role-specific dashboard variants (Manager/HR/CMD/Admin) — explicitly deferred, single generic dashboard is correct for now.
4. **Kaizen Submission Wizard & My Ideas** — ~~Backend `kaizens` module (create/get/update/delete/submit)~~, ~~frontend 7-step wizard with autosave and draft recovery~~ (done, 2026-07-08) — ~~`GET /kaizens` list + `GET /kaizens/:id/timeline`~~, ~~My Ideas list page + read-only detail page~~ (done, 2026-07-08, Milestone 5). Remaining: resuming a draft found via the My Ideas list (today only the single most-recent wizard-session draft is resumable, via localStorage), real file uploads (Cloudinary), duplicate-draft action — all deliberately deferred, see Kaizen Wizard module notes.
5. **Review & Scoring** — ~~Review queue, comments, workflow transitions through `WorkflowService`~~ (done, 2026-07-09, Milestone 6 — see the Review Workspace module entry). Remaining: evaluation engine (5-parameter scoring, `evaluations`/`evaluation_scores`) — the Approve/Reject actions built in Milestone 6 deliberately do not enforce the API spec's "Evaluation submitted with recommendation: APPROVE/REJECT" precondition yet, since that engine doesn't exist (see Known Limitations).
6. **Implementation & Business Impact** — Assignment, progress tracking, verification, business-impact recording.
7. **Notifications & Gamification** — Notification delivery, points ledger, achievements, leaderboard snapshots + refresh job. Once these exist: award the documented "+10 on submit" points, send the documented "notify dept manager on submit" notification (both intentionally not implemented in Milestone 4 — see Kaizen Wizard module notes), and upgrade `RecentActivityCard`/`AnnouncementsCard` on the dashboard from placeholders to real data.
8. **Analytics & Admin Panel** — Per-role dashboard variants belong here, reports/export, admin settings/users/departments/categories/announcements/audit logs. Real department/category management supersedes Milestone 4's bootstrap seed data.
9. **Knowledge Base & Polish** — KB publishing flow, search, responsive/dark-mode pass, accessibility pass, test coverage.

## Current Architecture
- **Frontend**: Next.js 16 (App Router), React 19, TypeScript, Tailwind CSS 4, shadcn/ui + Radix, TanStack Query, React Hook Form + Zod, Framer Motion, Sonner (toasts). Charting library specified (Recharts) in docs but **not installed**.
- **Backend**: Node.js + Express 5, TypeScript (ESM), Zod validation. No queue/Redis, no logger library, no Cloudinary SDK installed yet despite config helper.
- **Database**: PostgreSQL (Neon), Prisma ORM — schema written and migrated (`001_initial_schema`, verified against the live DB); seeded with 12 categories + 1 bootstrap department; CHECK constraints and full-text search still pending a follow-up migration.
- **Authentication**: Clerk (frontend `@clerk/nextjs`, backend `@clerk/express`) — JWT verification real on both sides; Clerk webhook verifies signatures and syncs `users`/`user_gamification`; `req.user` (with real DB role) populated on every authenticated backend request; frontend has a theme-aware custom-branded sign-in/sign-up and a working `useCurrentUser()` hook.
- **Storage**: Cloudinary (planned) — no SDK installed, no upload endpoint.
- **Deployment (per docs, not yet configured in repo)**: Frontend → Vercel, Backend → Railway, DB → Neon PostgreSQL.
- **Repo topology**: Two independent repositories (`kaizen-web`, `kaizen-api`), not a monorepo — this matches `02_FOLDER_STRUCTURE.md` but conflicts with the single-tree diagram in `00_ARCHITECTURE.md` (see Known Issues).

## Module Status

### Authentication
Status: Complete for MVP scope (Milestone 2)
Progress: ~90%
Notes: Frontend — `/sign-in` and `/sign-up` are now proper Next.js catch-all routes (`[[...sign-in]]`/`[[...sign-up]]`), fixing a real bug where Clerk's internal multi-step navigation (password reset, verification codes) would 404 under the old flat routes. Sign-in/up widgets are theme-aware (light/dark palette recomputed from `next-themes`, since Clerk `appearance` is evaluated at render time, not CSS custom properties) and use custom `localization` copy matching AUTH-001. `/forgot-password` now redirects into Clerk's own built-in "Forgot password?" flow inside `/sign-in` (the old version mounted a second, separate `<SignIn>` instance that dead-ended — Clerk's reset flow is internal widget state, not something a second component instance can drive). `useCurrentUser()` hook + `authService.getMe/updateMe` call the real backend. A real **Logout Flow** now exists per AUTH-001: `features/auth/components/logout-button.tsx` opens a confirmation `AlertDialog` (new shadcn primitive at `components/ui/alert-dialog.tsx`, backed by newly-added dependency `@radix-ui/react-alert-dialog`) before clearing the TanStack Query cache and calling Clerk's `signOut()`; wired into the header next to `UserButton`. "Redirect Login" after logout is handled by Clerk itself via the existing `NEXT_PUBLIC_CLERK_SIGN_OUT_FALLBACK_REDIRECT_URL` env var. Backend — Clerk webhook (`/webhooks/clerk`) verifies Svix signatures via `verifyWebhook()` and syncs `user.created/updated/deleted` into `users` (+ creates the paired `user_gamification` row); `attachUser` middleware resolves `req.user` from the DB on every authenticated request, with a just-in-time Clerk API fallback sync for when the webhook hasn't fired yet (local dev without a public webhook URL, or a race right after signup); `requireRole`/RBAC is now actually exercisable since `req.user.role` is real. `GET/PATCH /api/v1/me` return/update the real profile (matching the documented response shape exactly, including the gamification summary). Known, deliberate gaps: (1) "Remember Me" checkbox from AUTH-001 has no equivalent in Clerk's prebuilt `<SignIn>` — Clerk manages session persistence via Dashboard settings, not a per-login toggle, so this requirement can't be met without abandoning the prebuilt widget; (2) Clerk's own `<UserButton>` menu still has its own unconfirmed, built-in "Sign out" item — Clerk exposes no supported API to remove or gate it, so the confirmed logout flow lives in the separate button we added, not inside UserButton itself; (3) role assignment is Clerk-Dashboard-metadata-only until the Admin Panel exists; (4) per-role dashboard redirect targets (`/manager/dashboard` etc.) are intentionally not wired since those routes don't exist yet — building them is Dashboard-milestone scope, not Auth scope.

### Database
Status: Migrated, verified, and seeded (partially)
Progress: ~60%
Notes: `prisma/schema.prisma` defines all 14 enums and 28 models from `docs/engineering/01_DATABASE_SCHEMA.md` (corrected count — see Known Issues), with documented relationships, indexes, unique constraints, and cascade rules. Migration `001_initial_schema` is applied to the live Neon database and was independently verified: all 28 tables + 14 enums + 47 FKs + 69 indexes present; Prisma Client create/read/relational-include/cascade-delete/unique-constraint behavior all confirmed working against real data (test rows written and cleaned up). The `users`, `kaizens`, `kaizen_5w1h`, `kaizen_5why`, `kaizen_benefits`, `departments`, `categories`, `kaizen_number_sequences`, and `timeline_events` tables are now all actively used. `prisma/seed.ts` (Milestone 4) seeds the 12 real categories (with icons/descriptions) and one bootstrap "General" department — the latter is a pragmatic addition, not something the docs asked for (see Known Issues). Still open: no CHECK constraints (Prisma's schema DSL can't express them — needs a raw-SQL follow-up migration), no `search_vector` generated column/trigger/GIN index (deferred to `002_add_search_vector`), and scoring-parameters/achievements/platform-settings still aren't seeded (deferred to their own milestones).

### Backend APIs
Status: Auth + Kaizen Wizard + My Ideas + Review Workspace live, everything else scaffolded
Progress: ~32%
Notes: `GET /health`, `POST /webhooks/clerk`, `GET/PATCH /api/v1/me`, `GET /api/v1/departments`, `GET /api/v1/categories`, `GET /api/v1/kaizens` (list, self-scoped, search/filter/sort/paginate), `POST/GET/PATCH/DELETE /api/v1/kaizens/:id`, `POST /api/v1/kaizens/:id/submit`, `GET /api/v1/kaizens/:id/timeline`, `GET /api/v1/reviews/queue` (new, Milestone 6), and 7 new `/api/v1/kaizens/:id/review/*` and `/api/v1/kaizens/:id/comments*` routes are all mounted and functional. 12 other route files exist but are still 4-line stubs never wired into `src/routes/index.ts`. `src/modules/auth/*`, `src/modules/kaizens/*`, and (new) `src/modules/reviews/*` are real, working modules; `departments`/`categories` are minimal read-only (list-only, by design — write access is Admin Panel scope); `src/modules/workflow/workflow.service.ts` and `src/modules/audit/audit.service.ts` (new, Milestone 6) are now real too — `WorkflowService.transition()` and `AuditService.record()`, previously 4-line empty-class stubs — though `WorkflowService` only covers the review-workspace transition subset so far (see the Workflow Engine module entry); every other `src/modules/*` folder is still just `.gitkeep`.

### Dashboard
Status: Foundation complete (Milestone 3) — single generic dashboard, no per-role variants yet
Progress: ~35%
Notes: `app/(dashboard)/dashboard/page.tsx` now renders a real `DashboardView` (`components/dashboard/`) fed entirely by `useCurrentUser()` — no new API client, reuses the Milestone 2 hook exactly as instructed. Sections: `WelcomeHeader` (personalized greeting), `StatsCards` (Total Points / Ideas Submitted / Ideas Approved / Ideas Implemented / Current Rank, `—` for null), `ProfileSummaryCard` (avatar with initials fallback, role badge, department/job title with `—` fallback), `RecentActivityCard` and `AnnouncementsCard` (both intentionally placeholder — no backend module exists yet to feed them), `QuickActionsCard` (3 disabled actions with "Coming soon" tooltips). Loading state is a full skeleton (`DashboardSkeleton`); error state reuses `ErrorState` with retry; entrance animation via the existing `FadeIn` (Framer Motion) component — no new animation dependency. Four small UI primitives were added to `components/ui/` (`badge`, `avatar`, `tooltip`, `separator`) — all hand-rolled with **zero new npm packages**, since the spec explicitly said not to install unless required and none of these four needed Radix (unlike the AlertDialog added in Milestone 2, which genuinely needed focus-trap/portal behavior). Verified by rendering all components through a real (temporary, since-deleted) Next.js route with mock data covering both a fully-populated profile and an all-nulls profile — confirmed correct `—` fallbacks, correct avatar initials ("CP", "AR"), correct role badges, and no runtime/hydration errors in the rendered output; full authenticated-session verification (real Clerk login) wasn't possible in this environment (no browser automation tool available). Still not started: per-role dashboards (Manager/HR/CMD/Admin) — deliberately out of scope per the milestone brief ("build the dashboard foundation... extended in future milestones"); Recent Activity and Announcements have no real data source yet (blocked on Workflow/Admin Panel respectively).

### Kaizen Wizard (incl. My Ideas)
Status: Complete for Milestones 4–5 scope (Submission Wizard + My Ideas employee portal)
Progress: ~85%
Notes: End-to-end submission + retrieval flow, both repos.

**Submission (Milestone 4, 2026-07-08)** — Backend (`src/modules/kaizens/`): `createDraft` (auto-fills `departmentId` from the user's profile when omitted, matching the documented `POST /kaizens` contract exactly; generates the `KZN-YYYY-NNNNN` number at creation time since the schema requires it non-null even for drafts), `update` (owner-only, DRAFT/NEEDS_CHANGES-only, transactional nested upserts for 5W1H/5Why + replace-all for benefits), `getById`/`remove` (ownership + role-scoped), and `submit` (validates category/problem-statement/current-process/proposed-solution/all-six-5W1H-fields/all-five-5Why-levels/at-least-one-benefit before transitioning DRAFT→SUBMITTED, writes a `timeline_events` row). The submit transition is a narrow, single-purpose implementation living in the Kaizen service, deliberately **not** a generic `WorkflowService` — building the real state machine covering all 12 statuses is the Workflow Engine milestone's job. Two documented submit side effects are intentionally not implemented: notifying the department manager and the "+10 points" award (Notifications/Gamification milestones). Frontend (`features/kaizen/`): one React Hook Form + Zod instance spans all 7 steps (Basics → Process → 5 Why → 5W1H → Benefits → Attachments → Review), `trigger()`-based per-step validation, a draft created after Step 1 and PATCHed on every step transition plus a 30s interval (autosave), and refresh-safe recovery via a `localStorage` snapshot. "Business Impact"/"Estimated Savings" reuse `kaizen_benefits` with fixed type codes (`BUSINESS_IMPACT`, `ESTIMATED_SAVINGS`) rather than the separate `business_impacts` table (that's populated later, post-implementation, by managers/HR). Category cards use a hand-built icon registry with a fallback for anything unmapped. Two dependency-free UI primitives: `textarea`, `select`.

**My Ideas (Milestone 5, 2026-07-08)** — Backend: `GET /kaizens` (list) is deliberately scoped to `submitterId: requester.id` **always**, regardless of role — the documented API spec's fuller role-based scoping (Dept Manager sees department, HR/CMD/Super Admin see everything) is Review Workspace territory, out of scope here ("Do NOT implement reviewer workflows"). Supports comma-separated `status`, `categoryId`, `priority`, `search` (title, `problemStatement` — the closest schema equivalent to the milestone brief's "description", plus `kaizenNumber` since the doc explicitly wants that searchable too), `sort` (`newest`/`oldest`/`updated`), and `page`/`pageSize` (`limit` accepted as an alias, matching the milestone brief's example query literally). `GET /kaizens/:id/timeline` reuses the exact same ownership check as `getById`. Frontend: new `/kaizen` (My Ideas list — search with `useDebounce`, status/category/priority filters, sort, pagination, empty states for "no Kaizens at all" vs. "no results for these filters") and `/kaizen/[id]` (read-only detail: general info, 5W1H, 5 Why, benefits, attachments placeholder, timeline) routes; new sidebar nav item (`components/layout/foundation-nav.ts`, shared between `AppSidebar`/`MobileNav` rather than duplicated — the previous "My Ideas" entry in the still-unused `constants/navigation.ts` also had its href corrected from `/ideas` to `/kaizen`). Cards, not a table, per instruction to match the existing design system — nothing else in the app uses a data table yet (`components/data-table/*` are still `return null;` stubs) and cards handle mobile responsiveness more naturally. `KaizenStatusBadge` centralizes status→color via a new `KAIZEN_STATUS_BADGE_VARIANT` map in `constants/kaizen-status.ts`, using 2 new Badge variants (`warning`, `info`) wired to design tokens that already existed in `globals.css` but weren't exposed on the Badge component yet. `EmptyState` gained an optional `icon` prop (backward compatible) rather than building a one-off empty state just for this page.

Verified: full backend flow — list correctly scoped to the requester only (a second "intruder" user's Kaizen never appears), newest-first default, oldest/updated sort, status/category filters, search across title/problemStatement/kaizenNumber, pagination (page split + `limit` alias), `getById`/timeline ownership enforcement (403 for a non-owner) — all against the live database via a temporary script, every check passed. Frontend: all list/detail/filter/pagination/empty-state/skeleton components (including all 12 status badge variants and both empty-state copies, matching the milestone brief's exact required text) rendered through a real (temporary, deleted after) Next.js request with no runtime/hydration errors. Could not verify the full interactive authenticated click-through flow (typing in the search box, clicking a filter, clicking into a real Kaizen) — no browser automation tool available in this environment.

Not built, by design (explicitly out of scope per the milestone brief): reviewer actions, admin actions, editing an approved Kaizen, delete-after-submission, notifications, leaderboard, analytics. Also not built: resuming a DRAFT found via the My Ideas list (list/detail are read-only; only the single most-recent wizard-session draft is resumable, via localStorage — see Milestone 4 notes), the `POST /kaizens/:id/duplicate` endpoint, and real file uploads (the detail page's Attachments section always shows "No attachments" since nothing is ever persisted server-side yet).

### Review Workspace
Status: Complete for Milestone 6 scope (review queue, review actions, comments)
Progress: ~70%
Notes: Backend (`src/modules/reviews/`): `ReviewService.getQueue` (role-scoped — Department Manager sees their own department, HR/CMD/Super Admin see all, Employees are rejected by `requireRole("DEPARTMENT_MANAGER")` before the service ever runs; defaults to excluding `DRAFT` Kaizens unless a status filter is explicitly given; reuses `KAIZEN_LIST_SELECT`/`toListItem`/pagination helpers from the `kaizens` module rather than duplicating them), `startReview`/`approve`/`reject`/`requestChanges` (each calls `kaizenService.getById` first — reusing its view-permission check — then a private `assertCanManage` restricting the actual action to "Department Manager, same department" exactly per the API spec, then delegates the status change itself to the new `WorkflowService.transition()`), and `listComments`/`addComment`/`resolveComment` (reviewers can always comment; the submitter only while `NEEDS_CHANGES`; resolve is Department-Manager-only). Routes: `GET /api/v1/reviews/queue` (new `reviews.routes.ts`) plus 7 routes added to the existing `kaizens.routes.ts` (`POST /:id/review/{start,approve,reject,needs-changes}`, `GET/POST /:id/comments`, `PATCH /:id/comments/:commentId/resolve`). Frontend: new `/review` (queue: search, status/department/category/priority filters — department filter only shown to HR/CMD/Super Admin, since a Dept Manager's queue is already server-scoped — sort, pagination) and `/review/[id]` (detail) routes under `features/review/`; `KaizenDetailView` (Milestone 5) gained two optional slots (`actionSlot`, `extraContent`) so the review detail page reuses the same header/5W1H/5Why/Benefits/Timeline rendering instead of duplicating it, inserting a `ReviewActionPanel` (role/status-aware Start Review / Approve / Reject / Request Changes buttons, the latter three behind an `AlertDialog` confirmation with an optional notes field) and a `ReviewCommentsPanel` (list + add + resolve). A new role-aware "Review" nav item (Department Manager and above only) was added to the shared `foundation-nav.ts`.

Verified: full workflow exercised against the live database via a temporary script (mounting the real `kaizensRouter`/`reviewsRouter` with a stubbed `req.user`) — Employee blocked from the queue and from every review action (403); a Department Manager from a *different* department blocked from acting on or seeing the Kaizen in their queue; HR blocked from acting (but can still view/list it) per the spec's stricter "Department Manager only" actor scope; the full `SUBMITTED → UNDER_REVIEW → NEEDS_CHANGES` and a separate `SUBMITTED → UNDER_REVIEW → APPROVED`/`→ REJECTED` path, each confirmed via the returned status, the `timeline_events` row, and (for approve) the `audit_logs` row and `approvedAt` timestamp; an invalid transition (re-starting an already-`UNDER_REVIEW` Kaizen, approving from `NEEDS_CHANGES`) correctly rejected with `409 INVALID_STATE_TRANSITION`; comment visibility/authorship rules (submitter blocked outside `NEEDS_CHANGES`, allowed during it; manager can always comment and resolve); a `DRAFT` Kaizen confirmed to never appear in the queue. 21/21 checks passed; test data cleaned up, script deleted. `npm run typecheck`/`lint` clean in both repos (only the same 3 pre-existing, unrelated `kaizen-api` lint errors remain, now down to 1 — see Known Issues); `npm run build` (kaizen-web) succeeds with `/review` and `/review/[id]` both registered. Could not verify the interactive click-through flow in a real browser — no browser automation tool available in this environment (same disclosed limitation as every prior milestone).

Not built, by design (explicitly deferred — see Known Limitations below): the API spec's Approve/Reject precondition on a submitted Evaluation (the Scoring Engine doesn't exist yet); `PATCH /kaizens/:id/priority`; a "Begin Evaluation" action (no status transition corresponds to it without the Scoring Engine); threaded comment replies in the UI (the backend accepts `parentId`, matching the documented request shape, but the frontend always posts top-level comments); a separate review "dashboard" with its own stats/KPIs (interpreted "review dashboard" as the queue page itself, since the milestone brief gives no separate spec for reviewer-facing stats, unlike the real company Dashboard).

### Scoring
Status: Not Started
Progress: 0%
Notes: `features/scoring/*` (frontend) is empty. No `evaluations`/`evaluation_scores` read/write logic, no scoring parameters seeded, no evaluation UI. This is now the direct blocker for enforcing the Approve/Reject precondition documented in `02_API_SPECIFICATION.md` (see the Review Workspace module entry).

### Workflow Engine
Status: Partially started (Milestone 6) — review-workspace transitions only
Progress: ~20%
Notes: `workflow.service.ts` was a 4-line empty class ("will be implemented in the workflow phase") until Milestone 6, which gave it a real `transition(kaizenId, toStatus, actor, metadata)` matching the API spec's "Workflow Service (Internal)" contract (validate the transition, update `kaizens.status`, write a `timeline_events` row, write an `audit_logs` row via the also-newly-implemented `AuditService.record()`, emit a domain event) — but its transition table only covers `SUBMITTED→UNDER_REVIEW` and `UNDER_REVIEW→{APPROVED,REJECTED,NEEDS_CHANGES}`, the subset this milestone's review actions need. `KaizenService.submit`'s `DRAFT`/`NEEDS_CHANGES`→`SUBMITTED` transition (Milestone 4) was deliberately left as-is, not migrated to go through `WorkflowService` (see Technical Debt) — refactoring a completed milestone's code wasn't part of this milestone's brief. Later-lifecycle transitions (implementation, business impact, rewards, archive, publish) still have no transition-table entries at all.

### Notifications
Status: Not Started
Progress: 0%
Notes: `features/notifications/*` (frontend) and `notifications` module (backend) are both empty scaffolding. No `notifications` table, no delivery mechanism.

### Analytics
Status: Not Started
Progress: 0%
Notes: `features/analytics/*` (frontend) is empty; `components/charts/*` exist but are literal `return null;` stubs, and no charting library is installed. Backend `analytics` module is empty.

### Gamification
Status: Not Started
Progress: 0%
Notes: `features/gamification/*` (frontend) and `gamification` module (backend) are empty. `leaderboard-refresh.job.ts` is registered as a background job but its body is empty. No points ledger, achievements, or leaderboard snapshot logic exists.

### Admin Panel
Status: Not Started
Progress: 0%
Notes: `features/admin/*` (frontend) is empty; backend `admin` module is empty. No user/department/category management, scoring-parameter config, announcements, or audit-log viewer exists, despite `ADMIN-001`/`Super Admin` requirements being well-specified in docs.

### Knowledge Base
Status: Not Started
Progress: 0%
Notes: `features/knowledge-base/*` (frontend) is empty; backend `knowledge-base` module is empty. No `knowledge_base_entries` table, no publish flow, no search.

## Known Issues
- **Schema model count corrected**: earlier status entries said "26 models" for `prisma/schema.prisma` — the accurate count is **28** (organization 2 + users/auth 2 + kaizen_core 6 + review_and_scoring 4 + implementation 2 + business_impact 1 + gamification_mvp 5 + knowledge_base 1 + notifications 1 + platform 2 + audit_and_timeline 2). Verified against both the migration SQL (28 `CREATE TABLE`) and the live database catalog.
- **Fixed this milestone — sign-in/sign-up routing bug**: `app/(auth)/sign-in/page.tsx` and `sign-up/page.tsx` were plain (non-catch-all) routes while Clerk's `<SignIn>`/`<SignUp>` were configured with `routing="path"`, which requires a catch-all segment (`[[...sign-in]]`) so Clerk can push internal sub-routes (password reset, verification steps) without 404ing. Converted both to catch-all routes; `/forgot-password` (which mounted a second, disconnected `<SignIn>` instance that could never reach the reset form) now redirects into `/sign-in`'s own built-in forgot-password flow.
- **Documentation gap**: `docs/product/02_PRODUCT_REQUIREMENTS.md`'s own module list at the top (`DASH-002`, `KAIZEN-002`, `REVIEW-002`, `NOTIFY-001`, `LEADERBOARD-001`, `BADGE-001`, `PROFILE-001`, `ADMIN-001`, `SEARCH-001`) is never actually detailed in the document body — the body instead defines `SCORE-001`, `GAMIFY-001`, `WORKFLOW-001` (undeclared in the ToC). Admin Dashboard, My Ideas, Notifications, Profile, Settings, and Knowledge Base modules have **no detailed spec at all** to build or audit against.
- **Conflicting lifecycle definitions across docs**: `REVIEW-001`'s status enum, `IMPLEMENT-001`'s lifecycle, and `WORKFLOW-001`'s state machine each order/name stages differently (e.g. "Business Impact Review" vs. "Verified", presence/absence of "Rejected", "Submitted Again"). The Prisma-schema doc's `KaizenStatus` enum (12 values) is the most concrete version and should likely be treated as canonical when implementing.
- **Verification outcome mismatch**: `IMPLEMENT-001` defines Pending/Verified/Rejected; `WORKFLOW-001` adds an undefined "Needs Rework" outcome with no enum home.
- **Repo topology contradiction**: `00_ARCHITECTURE.md` diagrams a single unified tree; `02_FOLDER_STRUCTURE.md` explicitly mandates two separate repos (matches actual repo layout on disk). Treat the two-repo model as authoritative.
- **Color-token reuse**: Design system assigns "Red" to both Primary (CTAs) and Danger (errors/rejected), contradicting its own single-accent-per-component rule.
- **Dead/divergent code in kaizen-web**: `constants/navigation.ts` defines full role-based nav trees, but `app-sidebar.tsx`/`mobile-nav.tsx` use a separate hardcoded single-item list instead — the richer constant is currently unused.
- **Stray zero-byte files** at `kaizen-web` repo root (`agent.txt`, `agent_err.txt`, `login.txt`, `login_err.txt`, `out.txt`, `whoami.txt`, `whoami_err.txt`, `err.txt`) — leftover shell-redirection artifacts, not part of the app; harmless but worth cleaning up.
- **`kaizen-web` has its own nested `.git`** — confirm intended repo boundary before any tooling assumes a single top-level git repo across the whole workspace.
- **New — bootstrap "General" department**: `kaizens.department_id` is `NOT NULL`, but real department management is Admin Panel scope (not built). Without *some* department to assign, no Kaizen could ever be submitted, so Milestone 4 seeded a single "General"/`GEN` department as a pragmatic unblocker — not something any doc asked for. Replace/supersede this once the Admin Panel exists.
- **New — Department field simplified vs. doc**: AUTH's Step 2 spec (and KAIZEN-001) says Department should be "Editable only by privileged users" (auto-filled from profile, employees can't change it). Milestone 4 preselects the user's department but leaves the dropdown editable for everyone — there's only one department to pick from right now anyway (see bullet above), so the restriction would have no observable effect yet. Revisit once real, multiple departments exist.
- **New — Milestone 5's status filter uses the full 12-value `KaizenStatus` enum, not the milestone brief's literal 7-item list** (All/Draft/Submitted/Under Review/Approved/Rejected/Implemented). The brief's list doesn't map cleanly onto the real schema — "Implemented" isn't an actual status value (the closest are `IMPLEMENTATION_IN_PROGRESS`/`IMPLEMENTATION_COMPLETED`) — so the filter reuses the existing `KAIZEN_STATUS_LABELS` constant (single source of truth, already used elsewhere) instead of introducing a second, incomplete status vocabulary. Same reasoning as Milestone 4's step-count reconciliation.
- **New — transient database connectivity during Milestone 5**: mid-session, the Neon Postgres endpoint became briefly unreachable (`P1001: Can't reach database server`) for about 30–40 seconds, affecting even read-only commands like `prisma migrate status` — unrelated to any code change. Retrying after a short wait succeeded; this is consistent with a Neon compute cold-start/autoscale-from-zero delay, not a bug. Noted here in case it recurs and looks alarming in a future session.
- **New — the same transient Neon cold-start delay recurred during Milestone 6's verification script** (`P1001` on the first `prisma.department.upsert()` call); resolved the same way (retry after ~15s). Consistent with the Milestone 5 note above, not a new issue.
- **New — Milestone 6's Approve/Reject actions intentionally skip the API spec's documented precondition** ("Evaluation submitted with recommendation: APPROVE/REJECT") — enforcing it needs the Scoring Engine's `Evaluation` model, which doesn't exist yet (0% progress, see the Scoring module entry). A Department Manager can currently approve or reject directly from `UNDER_REVIEW` with only an optional free-text `notes` field. Revisit once the Scoring Engine milestone ships.
- **New — `PATCH /kaizens/:id/priority` was not implemented**, even though the API spec lists it under the same "Review" section as the actions this milestone built. It doesn't correspond to a status transition (priority is unrelated to `KaizenStatus`) and wasn't in the milestone brief's explicit action list (Start Review/Approve/Reject/Request Changes) or its reviewer-detail-page spec (which lists fields the reviewer should *see*, not edit) — deferred rather than guessed at.
- **New — "Begin Evaluation," mentioned as an example review action in the milestone brief, was not implemented.** It doesn't correspond to any `KaizenStatus` transition on its own — creating/editing an `Evaluation` record is Scoring Engine territory (a separate, not-yet-built milestone), not a workflow-status change. Building a real "Begin Evaluation" action means building the Scoring Engine first.
- **New — comment replies (`parentId`) are backend-only.** The API accepts and validates `parentId` exactly per the documented request shape, and `ReviewComment`'s self-relation supports threading, but the Milestone 6 frontend always posts top-level comments (`parentId: null`) — no "reply to this comment" UI exists yet. Not required by the milestone brief's comments section, which only asks to "implement reviewer comments... persist them... display them," not build threading UI.
- **New — "review dashboard" (mentioned in the Milestone 6 frontend requirements) was interpreted as the review queue page itself**, not a separate reviewer-facing stats/KPI page — the milestone brief gives a full, separate spec for the review *queue* and *detail* page, but no equivalent spec for reviewer stats (unlike the real company Dashboard, built in Milestone 3 with its own detailed brief). No aggregate-stats endpoint was built as a result.

## Technical Debt
- `prisma/schema.prisma` cannot express three documented CHECK constraints (`kaizen_5why.level` 1–5, `evaluation_scores.score` 0–10, `implementations.progress_percent` 0–100) — Prisma's schema DSL has no CHECK support. These must be added via a hand-edited raw-SQL migration; until then those bounds are enforced only at the application layer, not the database.
- `knowledge_base_entries.search_vector` is modeled as `Unsupported("tsvector")` (excluded from the Prisma Client) — the actual generated column, its update trigger, and its GIN index still need to be added by hand-written SQL in a dedicated migration (the doc itself plans this as a separate `002_add_search_vector` migration, not part of the initial schema).
- `services/api/index.ts` (`export const apiServices = {}`) and `lib/api-client.ts`/`lib/clerk.ts` are fully built infra with zero callers — will need real service modules per feature domain rather than growing this file ad hoc.
- No test framework installed in either repo — testing strategy needs to be decided before the module build-out grows large enough that retrofitting tests is painful. This is now more pressing: the auth module (webhook verification, sync logic, JIT fallback) is exactly the kind of logic that benefits most from regression tests, and it currently has none.
- In-memory rate limiter is not distributed-safe (resets per instance) — fine for single-instance dev, needs revisiting before multi-instance deployment.
- **New**: `AuthService.resolveOrSyncUser`'s just-in-time fallback calls the Clerk Backend API on every request from a user the local DB doesn't yet know about (not just once) until the webhook or JIT sync succeeds in writing the row — fine for the expected case (webhook usually beats the user's first API call), but if `CLERK_WEBHOOK_SECRET` is ever left unset in an environment that also has real traffic, every request from new users pays an extra Clerk API round-trip indefinitely instead of failing loudly. Worth a startup warning log if the webhook secret is unset in a non-development environment.
- **New**: Repo-wide Prettier drift — `npm run format:check` in `kaizen-web` currently flags 52 pre-existing files (not touched this session) as non-compliant. Not introduced by Milestones 1–2, but worth a dedicated formatting pass before it grows further.
- **New**: 3 pre-existing ESLint errors in `kaizen-api` (`error-handler.ts` unused param, `audit.service.ts` and `workflow.service.ts` empty classes) — confirmed pre-existing (not introduced by Milestones 1–2), left untouched since fixing them is outside Authentication scope and those modules aren't part of this milestone.
- **New**: `components/ui/tooltip.tsx` (Milestone 3) is a hand-rolled CSS-only tooltip (group-hover/group-focus-within), not `@radix-ui/react-tooltip` — deliberate, to avoid an unrequired new dependency (the milestone brief explicitly said not to install packages without asking). Fine for its current use (a static "Coming soon" label), but it has no `Escape`-to-dismiss, no delay/debounce, and — the one real limitation worth knowing — it never triggers on the `QuickActionsCard` buttons specifically, because those buttons use the native `disabled` attribute, which removes them from the Tab order entirely (`group-focus-within` has nothing to focus). Not fixed: these are placeholder buttons slated for replacement once the Kaizen Wizard/Gamification milestones ship real actions, so the proper fix (`aria-disabled` + explicit `tabIndex` instead of `disabled`) was judged not worth the complexity for UI that's temporary by design. Revisit if this Tooltip is reused on a *permanently* disabled-but-explainable control.
- **New**: `KaizenService.submit`'s validation and the status-transition logic live directly in the Kaizen module rather than a shared `WorkflowService` (which doesn't exist — see the module notes above). When the Workflow Engine milestone builds the real state machine, this narrow transition should be migrated into it rather than left as a parallel, inconsistent path.
- **New**: Kaizen Wizard autosave has no conflict handling for two browser tabs editing the same draft — the last PATCH to land wins silently. Unlikely in practice (one user, one wizard session) but worth knowing before this pattern is reused elsewhere.
- **New**: The wizard's draft-recovery `localStorage` snapshot can drift from the server if a PATCH succeeds but `writeStoredDraft` somehow doesn't (e.g. localStorage quota, private browsing) — recovery would then resume slightly stale local values even though the server has newer ones. Low risk (both are written in the same code path immediately after each other) but not transactional across the two.
- **New (Milestone 6)**: `KaizenService.submit`'s `DRAFT`/`NEEDS_CHANGES`→`SUBMITTED` transition (Milestone 4) still does not go through `WorkflowService.transition()`, even though `WorkflowService` is now real (Milestone 6). Migrating it was out of scope for this milestone (refactoring completed Milestone 4 code wasn't requested) but should happen once a milestone touches that code path anyway, so all status transitions eventually funnel through one engine as the API spec intends.
- **New (Milestone 6)**: `WorkflowService`'s transition table (`REVIEW_TRANSITIONS` in `workflow.service.ts`) only covers the two review-workspace edges this milestone needed. It is not yet a general state machine for all 12 `KaizenStatus` values — later milestones (Implementation, Business Impact, Rewards, Archive, Knowledge Base) will need to extend this table rather than build a second, parallel transition mechanism.
- **New (Milestone 6)**: The review queue's default status filter (everything except `DRAFT`) is a judgment call, not something the API spec states explicitly — the spec only says the queue's `status` param is optional and comma-separated, without defining default behavior. Revisit if a future milestone's UX expectations differ (e.g. wanting `APPROVED`/`REJECTED` Kaizens to disappear from the queue by default rather than requiring an explicit filter).

## Pending Decisions
- Final-score aggregation method for multi-reviewer evaluations: MVP uses simple average; docs mention Median/Weighted-Average as deferred options with example CMD/HR/Manager weight split — needs a decision before the scoring engine is built, since the schema/API should accommodate the eventual method.
- Which canonical Kaizen lifecycle/state list to implement, given the three conflicting versions across `REVIEW-001`, `IMPLEMENT-001`, and `WORKFLOW-001` docs (recommend adopting the Prisma-schema doc's 12-value `KaizenStatus` enum as ground truth and reconciling the narrative docs to match).
- Charting library choice: docs/architecture specify Recharts, but it isn't installed and `components/charts/*` are inert stubs — confirm before building analytics.
- **New**: AUTH-001 specifies a "Remember Me" checkbox on login; Clerk's prebuilt `<SignIn>` has no equivalent control (session persistence is a Clerk Dashboard-level setting, not a per-login user choice). Decide whether to (a) accept this as a documented deviation, (b) configure Clerk Dashboard session settings to approximate "always remember," or (c) drop the prebuilt widget for a custom `useSignIn()`-based form just to add this one control — (c) is a large scope increase for one checkbox, not recommended.
- **New**: Role assignment currently requires manually editing `public_metadata.role` in the Clerk Dashboard per user. Acceptable as a stand-in until the Admin Panel exists, but confirm this is fine for whoever is onboarding early test users.
- **New**: Whether the single "General" bootstrap department is acceptable to keep using until the Admin Panel ships real department management, or whether a small set of real departments should be seeded sooner (see Known Issues).
- **New**: Confirm the "Business Impact"/"Estimated Savings" → `kaizen_benefits` mapping decision (see Kaizen Wizard module notes) is the right long-term home, versus adding dedicated schema fields later — a schema change now was avoided since it wasn't requested, but this is worth a deliberate call before Review & Scoring or Analytics start reading benefit data and need to distinguish these two from regular expected-benefit entries.
- **New**: `GET /kaizens` currently ignores role entirely and always scopes to the requester — confirm this is fine to leave as-is until the Review Workspace milestone adds the documented role-based scoping (Dept Manager → department, HR/CMD/Super Admin → all), rather than half-building that scoping logic now with no reviewer UI to exercise it.
- **New**: Whether "resume a draft from the My Ideas list" (clicking a DRAFT-status card and landing back in the wizard at the right step) is worth a dedicated follow-up before Review & Scoring, or whether it's fine to leave the list/detail pages read-only for now — today a user can only ever resume the *one* most recent wizard session (via the Milestone 4 localStorage mechanism), not an arbitrary older draft found via browsing the list.
- **New**: Whether the Approve/Reject evaluation precondition (documented in `02_API_SPECIFICATION.md`, not enforced by Milestone 6) should be added retroactively to `ReviewService.approve`/`reject` once the Scoring Engine ships, or whether the simpler direct-approval flow built now should just remain as-is going forward — confirm before the Scoring Engine milestone starts, since it changes `ReviewService`'s behavior rather than only adding new endpoints.
- **New**: Whether `PATCH /kaizens/:id/priority` (documented, not built — see Known Issues) is worth adding as a small standalone follow-up, or should wait and be bundled into a future milestone that touches the review detail page again.

## Next Recommended Task
**Interactive Scoring Engine** (the remaining half of the roadmap's "Review & Scoring" item — Review Workspace itself, Milestone 6, is now done) — a Department Manager can now start a review, comment, and approve/reject/request-changes directly, but the API spec's documented Approve/Reject precondition ("Evaluation submitted with recommendation: APPROVE/REJECT") and the 5-parameter scoring UI don't exist yet, so every approval today is judgment-only with no structured score behind it. `req.user.role`/`req.user.departmentId` (real since Milestone 2), the `kaizens`/`evaluations`/`evaluation_scores`/`scoring_parameters` schema (real since Milestone 1), and `ReviewService`/`WorkflowService` (real since Milestone 6, ready to be extended) mean nothing else needs to happen first.

Required prerequisite: seed the 5 scoring parameters (`prisma/seed.ts`) before starting, since the scoring engine needs them to exist — cheap, same pattern as Milestone 4's category seeding. This was optional before Milestone 6; it is now a hard blocker since there is no other milestone left before Scoring.

## Last Updated
2026-07-09 — Milestone 6 (completed in full): Review Workspace. Backend — `WorkflowService.transition()` and `AuditService.record()` (previously empty stubs) now real, matching the API spec's "Workflow Service (Internal)" contract for the `SUBMITTED→UNDER_REVIEW→{APPROVED,REJECTED,NEEDS_CHANGES}` subset; new `reviews` module (`ReviewService`, reusing `KaizenService`/`kaizen.mapper` helpers rather than duplicating them) backing `GET /api/v1/reviews/queue` (new route file, role-scoped: Dept Manager → own department, HR/CMD/Super Admin → all, Employee → 403) and 7 new routes on the existing `kaizens.routes.ts` (`POST /:id/review/{start,approve,reject,needs-changes}`, `GET/POST /:id/comments`, `PATCH /:id/comments/:commentId/resolve`). Frontend — new `/review` (queue: search, status/department/category/priority filters, sort, pagination) and `/review/[id]` (detail) routes; `KaizenDetailView` gained two optional slots (`actionSlot`, `extraContent`) so the detail page's 5W1H/5Why/Benefits/Timeline sections are reused, not duplicated, for the reviewer-facing `ReviewActionPanel` (Start Review / Approve / Reject / Request Changes, the latter three behind a confirmation dialog with an optional notes field) and `ReviewCommentsPanel`; a role-aware "Review" nav item (Dept Manager and above only). Deliberately deferred, and documented above: the Approve/Reject evaluation precondition and a "Begin Evaluation" action (both need the not-yet-built Scoring Engine), `PATCH /kaizens/:id/priority`, and comment-reply UI (backend supports `parentId`, frontend doesn't expose it yet). Verified: 21/21 checks passed against the live database via a temporary script covering RBAC (Employee/wrong-department-manager/HR all correctly blocked from acting, HR/company-wide correctly allowed to view), every transition (including two rejected-as-invalid transitions, confirmed `409 INVALID_STATE_TRANSITION`), timeline events, the `audit_logs` row and `approvedAt` timestamp on approval, and comment authorization rules; test data cleaned up, script deleted. `npm run typecheck`/`lint` clean in both repos (`kaizen-api` down to 1 pre-existing unrelated lint error, from 3, since this milestone filled in the two previously-empty `workflow`/`audit` stub classes that were causing 2 of them); `kaizen-web`'s `npm run build` succeeds with both new routes registered. Hit the same ~30–40s transient Neon cold-start delay as Milestone 5 mid-verification (unrelated to any code, resolved on retry). No new npm packages installed; no Prisma schema changes. Awaiting approval before starting the next milestone (Interactive Scoring Engine).
